# Worker Task — Ohmdal Authored Overnight Batch

## Worker

Primary executor: **Gemini 3.7 Flash High / Antigravity CLI / effort high / workspace-write**.

Run from an isolated worktree/branch based on the latest `explore/ohmdal-3D`. Do not use Codex Sol. MiniMax M3/GMI may be called through the existing repo runner for bounded technical-art questions. Codex Luna may be used only if an already-working local invocation exists and the scope is purely mechanical; do not invent a new wrapper tonight.

## Authority rule

You are a builder, not the acceptance authority. You may implement, test, capture, commit and push candidate milestones. You must **not** mark A4/A4B/A5/A6 `passed`, must not set the loop `complete`, and must not claim final visual acceptance.

## Starting point

The interrupted Codex work was pushed. Inspect before editing:

- `b923ef7` — partial A4 Castle work;
- `agent-work/loops/ohmdal-arco1-authored-pass/state.json`;
- `agent-work/tasks/ohmdal-arco1-authored-pass.md`;
- `docs/20-worlds/ohmdal/production/OHMDAL_NAVIGATION_COLLISION_CONTRACT.md`;
- `docs/20-worlds/ohmdal/production/OHMDAL_SCENIC_RENDERING_POLICY.md`.

A0–A3 are accepted. A3 already proved NVIDIA GTX 1660 Ti hardware FAST capture and Golden Path 22/22. Do not reopen them except regression fixes caused by your work.

## Overnight order

### O1 — Baseline verification

Run focused checks first:

- authored-loop validator;
- TypeScript/build;
- relevant current A4 Castle tests;
- Golden Path if reasonably bounded.

If the pushed partial state is broken, repair only what is necessary to restore its intended A4 candidate before proceeding.

### O2 — Finish A4 Castle candidate

Continue the partial implementation; do not restart it.

Required candidate outcomes:

- Castle reads as civic distribution stronghold/substation, not generic fantasy room;
- branch state, isolators, return continuity, protection trip and service consequences remain driven by the real model;
- no shared-material-per-frame mutation or permanent copper glow;
- preserve current parallel/mixed valid alternatives;
- produce/update canonical Castle FAST captures;
- run focused tests + build + Golden Path if runtime/navigation changed;
- write an evidence report under `agent-work/reports/workers/`;
- commit candidate A4 separately.

Do not run the existing A4 Gemini review as self-acceptance. You may prepare its evidence for a future separate read-only session.

### O3 — A4B Navigation + Scenic Shell candidate

This is mandatory production hardening based on human playtest.

Implement the contracts, favoring the smallest deterministic solution:

1. zone-local collision ownership/registry;
2. couple load-bearing solid authored geometry with collision or enforce an equivalent invariant;
3. transition spawn anchors using `position + lookAt/directionIntoZone` rather than scattered target yaw literals;
4. initial Portal arrival faces into Plaza/Ohm;
5. all major Arco I transitions face into their destination and do not immediate ping-pong;
6. collision debug diagnostic/test hook;
7. automated wall-challenge and door-facing tests;
8. close accidental interior shell holes where large sky-dome void is visible;
9. implement minimal scenic layering/proxy support only where needed to prove the policy; do not attempt final sky art tonight;
10. preserve zone loading and performance.

Run Golden Path after this candidate. Capture at least Taller/interior, Castle entry and one outdoor/transition view showing correct enclosure/facing. Commit A4B separately.

### O4 — Continue with A5 candidate only if O2/O3 are green

Author Forja + Terrazas support forms/dressing from existing region contract. Priority is legible thermal load/current/protection versus irrigation/pump service, not final hero art.

Use MiniMax M3 at most once for one exact bounded technical-art question if it saves implementation time. If used, supply exact files/interfaces and update `agent-work/reports/minimax-gmi/EVALUATION.md` honestly.

Commit A5 candidate separately.

### O5 — Continue with A6 candidate only if A5 candidate is green and time remains

Author Faro/Lago/return support pass. Keep existing DC calibration truth; do not invent RC/transient mechanics or dialogue.

Commit A6 candidate separately.

## MiniMax use tonight

M3 is a specialist, not your planner. Good prompts:

- one water/heat/electrical event;
- one shader/state-cue mapping;
- one bounded code recombination question.

Bad prompts:

- audit the whole runtime;
- design the entire zone;
- generate a large multi-file package.

Use the existing runner and verify every API/claim yourself.

## Performance/capture

Use FAST hardware capture for iteration and preserve renderer diagnostics. Do not claim hardware results unless `softwareRendered=false`. Do not weaken the FULL acceptance path.

## Do not do

- no Codex Sol;
- no engine/dependency upgrade;
- no Meshy/Tripo spend;
- no canon/curriculum changes;
- no final dialogue invention;
- no worker self-approval;
- no global rewrite of PlayCanvas runtime just to make code prettier;
- no deleting accepted baselines.

## Stop conditions

Stop and report instead of guessing if:

- a hero requires contradictory/insufficient identity reference;
- a gameplay/topology change is needed;
- a major dependency/engine change seems necessary;
- paid spend is required;
- three bounded attempts fail to improve a blocker;
- Golden Path becomes irreparably broken within the allowed scope.

## Final overnight report

Create `agent-work/reports/workers/ohmdal-authored-overnight.md` containing:

- starting commit;
- candidate commits created;
- files/scopes changed per candidate;
- tests/build/Golden Path results;
- capture paths + renderer/GPU diagnostics;
- known collision/navigation/scenic debt left;
- MiniMax calls and whether they saved time;
- exact current git status/branch;
- what ChatGPT/Sol should review first in the morning.

Push the worker branch. Leave the canonical authored-pass stage statuses unaccepted.
