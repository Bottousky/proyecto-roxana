OpenAI Codex v0.139.0
--------
workdir: C:\YO\Proyectos\Roxana claude
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: medium
reasoning summaries: none
session id: 019ef183-9fd1-7c50-ad9f-0701c1a3143f
--------
user
Implementá los 3 cambios descritos en docs/spec-lote-bc.md, al pie de la letra.

Antes de tocar cada puzzle, leé su vista (src/puzzles/<nombre>.ts) y su modelo para usar lo que ya exporta (no inventes valores ni toques modelos). Patrón a calcar: el panel de predicción de src/puzzles/chain.ts y la legibilidad por estado de src/puzzles/distributor.ts. Son 3 archivos de VISTA independientes: clock.ts, singlestone.ts, lighthouse.ts. Tocá SOLO esas vistas (y styles.css si necesitás un estilo chico). NO toques modelos, NO toques rooms.ts, NO commitees. El texto del juego se copia textual de la spec.

Al terminar, corré `npm run build` y asegurate de que pase. Reportá en pocas líneas qué cambiaste en cada archivo.

codex
Voy a leer la spec y las vistas/modelos relevantes antes de editar. También voy a mirar los dos patrones que marcaste para copiar estructura y estilo sin tocar lógica de modelo.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'docs/spec-lote-bc.md'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/chain.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/distributor.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg --files src | rg \"(clock|singlestone|lighthouse|styles\\.css)"'$"' in C:\YO\Proyectos\Roxana claude
 succeeded in 1295ms:
import { sfxClick, sfxDim, sfxOk, sfxWin } from '../audio';
import { benchActions, openBench } from '../ui/bench';
import { gaugeSVG, ohmProbe, ohmWidgetHTML, setGauge, setOhmState } from './common';
import {
  CHAIN_MAX_LAMPS,
  CHAIN_PUSHES,
  type ChainBand,
  type ChainPush,
  type ChainState,
  type RemovalGuess,
  type RiverGuess,
  addChainLamp,
  chainBand,
  chainReading,
  chainRiver,
  chainSegmentIds,
  createChainState,
  measureChainSegment,
  predictRemoval,
  predictRiver,
  removeChainLamp,
  setChainPush,
} from './chainModel';

type Phase = 'predict-river' | 'measure' | 'predict-removal' | 'remove' | 'restore';

const RIVER_OPTIONS: { key: RiverGuess; label: string }[] = [
  { key: 'antes', label: 'Antes de la primera' },
  { key: 'despues', label: 'DespuÃ©s de la Ãºltima' },
  { key: 'igual', label: 'Igual en todos lados' },
];

const REMOVAL_OPTIONS: { key: RemovalGuess; label: string }[] = [
  { key: 'iguales', label: 'Siguen igual' },
  { key: 'mas', label: 'Brillan mÃ¡s' },
  { key: 'apagan', label: 'Se apagan todas' },
];

const MEASURED_BODY =
  '<b>Edda:</b> Â«Esperaba que fuera bajandoâ€¦ PensÃ© que la primera lÃ¡mpara se quedaba con la mejor parte.Â»<br/>' +
  '<b>Ohm:</b> Â«Reparto en fila: no existe. Fila = un solo rÃ­o.Â»<br/>' +
  '<b>Consejera:</b> <i>(anotando, incÃ³moda)</i> Â«El instrumento estarÃ¡ descalibrado.Â»<br/>' +
  '<b>Ohm:</b> Â«El instrumento los escucha.Â»';

const REMOVED_BODY =
  '<b>Lumen:</b> Â«Â¡La fila entera muere por un soldado! En mis tiempos a eso lo llamÃ¡bamos diseÃ±o solemne.Â»<br/>' +
  '<b>Edda:</b> Â«Yo lo llamarÃ­a rehÃ©n.Â»';

const ADDED_DIALOGUE =
  '<b>Ohm:</b> Â«MÃ¡s frenos en fila: frenos sumados. RÃ­o menor para todos.Â»';

const SOLVED_DIALOGUE =
  '<b>Consejera:</b> Â«Seis lÃ¡mparas. MÃ¡s que las cuatro que yo dejÃ©â€¦ y brillan MÃS, no menos. Â¿CÃ³mo?Â»<br/>' +
  '<b>Edda:</b> Â«No ahorraba quitando. Faltaba empuje, no sobraban lÃ¡mparas. En la fila el rÃ­o no se reparte: lo comparten entero. MÃ¡s empuje, y todas brillan a la vez.Â»<br/>' +
  '<b>Ohm:</b> Â«Gasto en el camino: cero. Siempre fue cero.Â»';

export function abrirChain(onSolved: () => void, practica = false): void {
  openBench(
    'La Cadena',
    'Un solo rÃ­o, frenos que se suman, fragilidad de la fila.',
    (bench) => {
      let state = createChainState();
      let solved = false;
      let busy = false;
      let blackoutTimer: number | undefined;

      // Cristal de Empuje (sÃ³lo se habilita al restaurar la GalerÃ­a).
      const sourceTray = document.createElement('div');
      sourceTray.className = 'bench-tray chain-sources';
      sourceTray.innerHTML = '<span class="tray-label">Cristal de Empuje:</span>';
      const sourceButtons = new Map<ChainPush, HTMLButtonElement>();
      for (const push of CHAIN_PUSHES) {
        const button = document.createElement('button');
        button.className = 'fuente-opt chain-crystal';
        button.textContent = `Empuje ${push}`;
        button.addEventListener('click', () => {
          if (busy || solved || phaseOf(state) !== 'restore') return;
          sfxClick();
          state = setChainPush(state, push);
          render(false);
          bench.setStatus(restoreFeedback(state));
          finishIfSolved();
        });
        sourceButtons.set(push, button);
        sourceTray.appendChild(button);
      }
      bench.root.appendChild(sourceTray);

      // Panel de predicciÃ³n (predecir â†’ observar â†’ explicar).
      const predict = document.createElement('div');
      predict.className = 'bench-predict';
      bench.root.appendChild(predict);

      const stage = document.createElement('div');
      stage.className = 'bench-stage chain-stage';
      stage.innerHTML = `
        <div class="chain-circuit">
          <div class="chain-source">EMPUJE</div>
          <div class="chain-wire live"></div>
          <div class="chain-slots" aria-label="Fila de lÃ¡mparas"></div>
          <div class="chain-wire live"></div>
          <div class="chain-return">RETORNO</div>
        </div>
        <div class="chain-instruments">
          <div class="chain-gauge">
            ${gaugeSVG(1.6, 0.9, 4)}
            <div class="chain-gauge-label">brillo de la fila</div>
            <div class="chain-band"></div>
          </div>
          ${ohmWidgetHTML()}
        </div>`;
      bench.root.appendChild(stage);

      const probeHost = document.createElement('div');
      probeHost.className = 'chain-probe-host';
      bench.root.appendChild(probeHost);

      const goal = document.createElement('div');
      goal.className = 'bench-goal';
      bench.root.appendChild(goal);

      let actions: Record<string, HTMLButtonElement>;

      const finishIfSolved = () => {
        if (practica || solved || !state.solved) return;
        solved = true;
        sfxOk();
        sfxWin();
        bench.setStatus(SOLVED_DIALOGUE);
        actions['Alejarse'].classList.add('hidden');
        actions['Continuar'].classList.remove('hidden');
        render(false);
      };

      const chooseRiver = (guess: RiverGuess) => {
        if (busy || solved || state.predictions.river) return;
        sfxClick();
        state = predictRiver(state, guess);
        const label = RIVER_OPTIONS.find((option) => option.key === guess)!.label;
        bench.setStatus(
          `<b>Dijiste:</b> Â«${label}Â». Ahora llama a Ohm a medir en varios puntos de la fila.`,
        );
        render(false);
      };

      const chooseRemoval = (guess: RemovalGuess) => {
        if (busy || solved || state.predictions.removal) return;
        sfxClick();
        state = predictRemoval(state, guess);
        const label = REMOVAL_OPTIONS.find((option) => option.key === guess)!.label;
        bench.setStatus(`<b>Dijiste:</b> Â«${label}Â». Ahora saca una lÃ¡mpara y mira la fila.`);
        render(false);
      };

      const doMeasure = (segmentId: string) => {
        if (busy || solved) return;
        const hadExperience = state.experiences.measuredSameRiver;
        const rio = chainReading(state.push, state.lampCount, segmentId);
        state = measureChainSegment(state, segmentId);
        if (!hadExperience && state.experiences.measuredSameRiver) {
          const prefix =
            state.predictions.river === 'igual'
              ? '<b>Ohm:</b> Â«Lo anticipaste: igual en cada punto.Â»<br/>'
              : '<b>Ohm:</b> Â«Esperabas un extremo. Mira: el rÃ­o no cambia de un punto a otro.Â»<br/>';
          bench.setStatus(prefix + MEASURED_BODY);
        } else {
          bench.setStatus(`<b>MediciÃ³n:</b> rÃ­o ${formatRiver(rio)}.`);
        }
        render(false);
        finishIfSolved();
      };

      const removeLamp = () => {
        if (busy || solved) return;
        const change = removeChainLamp(state);
        if (!change.interrupted) return;
        const wasFirstRemoval = !state.experiences.removedLamp;
        const predicted = state.predictions.removal;

        sfxClick();
        sfxDim();
        state = change.state;
        busy = true;
        render(true);
        if (wasFirstRemoval) {
          const prefix =
            predicted === 'apagan'
              ? '<b>Edda:</b> Â«Lo sabÃ­as: se apagan todas.Â»<br/>'
              : '<b>Edda:</b> Â«Â¡Se apagaron TODAS! No sÃ³lo la que saquÃ©.Â»<br/>';
          bench.setStatus(prefix + REMOVED_BODY);
        }

        blackoutTimer = window.setTimeout(() => {
          busy = false;
          render(false);
          finishIfSolved();
        }, 600);
      };

      const addLamp = () => {
        if (busy || solved) return;
        const hadExperience = state.experiences.addedLamp;
        const change = addChainLamp(state);
        if (change.state === state) return;

        sfxClick();
        state = change.state;
        render(false);
        if (!hadExperience) bench.setStatus(ADDED_DIALOGUE);
        else bench.setStatus(restoreFeedback(state));
        finishIfSolved();
      };

      const renderPredict = (phase: Phase) => {
        if (phase === 'predict-river') {
          predict.classList.remove('hidden');
          predict.innerHTML = '<p class="bench-predict-q"><b>Ohm:</b> Â«Antes de medir: Â¿dÃ³nde crees que corre mÃ¡s rÃ­o?Â»</p>';
          appendPredictButtons(predict, RIVER_OPTIONS, (key) => chooseRiver(key));
        } else if (phase === 'predict-removal') {
          predict.classList.remove('hidden');
          predict.innerHTML = '<p class="bench-predict-q"><b>Ohm:</b> Â«Si saco una lÃ¡mpara, Â¿quÃ© crees que les pasa a las otras?Â»</p>';
          appendPredictButtons(predict, REMOVAL_OPTIONS, (key) => chooseRemoval(key));
        } else {
          predict.classList.add('hidden');
          predict.innerHTML = '';
        }
      };

      const renderProbes = (phase: Phase) => {
        const segments = chainSegmentIds(state.lampCount).map((id) => ({
          id,
          label: segmentLabel(id, state.lampCount),
        }));
        const probe = ohmProbe(
          segments,
          (id) => `RÃ­o: ${formatRiver(chainReading(state.push, state.lampCount, id))}.`,
          (_reading, segment) => doMeasure(segment.id),
        );
        const measuringEnabled = !solved && !busy && phase !== 'predict-river';
        probe.element.querySelectorAll('button').forEach((button) => {
          button.disabled = !measuringEnabled;
        });
        probeHost.replaceChildren(probe.element);
      };

      const renderGoal = (phase: Phase) => {
        if (phase === 'measure') {
          const segments = chainSegmentIds(state.lampCount);
          const done = segments.filter((id) => state.measuredSegments.includes(id)).length;
          goal.textContent = `Mide la fila con Ohm, punto por punto (${done} de ${segments.length}).`;
          return;
        }
        goal.textContent = GOAL_TEXT[phase];
      };

      const render = (forceOff: boolean) => {
        const phase = phaseOf(state);
        const blackedOut = forceOff;
        const river = chainRiver(state.push, state.lampCount);
        const brillo = blackedOut ? 0 : river;
        const glow = blackedOut ? 0 : Math.max(0.18, Math.min(1, river / 2));
        const glowRadius = Math.round(20 * glow);

        const slots = stage.querySelector<HTMLElement>('.chain-slots')!;
        slots.innerHTML = '';
        for (let index = 0; index < CHAIN_MAX_LAMPS; index++) {
          const occupied = index < state.lampCount;
          const socket = document.createElement('button');
          socket.className =
            'chain-socket ' + (occupied ? 'occupied' : 'available') + (blackedOut ? ' off' : '');
          const canRemove = phase === 'remove' || phase === 'restore';
          const canAdd = phase === 'restore';
          socket.disabled = solved || busy || (occupied ? !canRemove : !canAdd);
          socket.setAttribute('aria-label', occupied ? 'Quitar una lÃ¡mpara' : 'Agregar una lÃ¡mpara');
          socket.innerHTML = occupied
            ? `<span class="chain-bulb" style="--lamp-glow:${glow};--lamp-radius:${glowRadius}px"></span>
               <span class="chain-socket-label">L${index + 1}</span>`
            : '<span class="chain-plug">+</span>';
          socket.addEventListener('click', () => {
            if (occupied) removeLamp();
            else addLamp();
          });
          slots.appendChild(socket);
        }

        stage.classList.toggle('blackout', blackedOut);
        stage.querySelectorAll('.chain-wire').forEach((wire) => {
          wire.classList.toggle('live', !blackedOut);
        });
        setGauge(stage, brillo, 4);
        stage.querySelector<HTMLElement>('.chain-band')!.textContent = bandLabel(
          blackedOut ? 'casi-nada' : chainBand(state.push, state.lampCount),
        );
        setOhmState(stage, ohmStateFor(state, blackedOut));

        sourceButtons.forEach((button, push) => {
          button.classList.toggle('selected', push === state.push);
          button.disabled = solved || busy || phase !== 'restore';
        });

        renderPredict(phase);
        renderProbes(phase);
        renderGoal(phase);
      };

      actions = benchActions(bench.root, [
        {
          label: 'Alejarse',
          onClick: () => {
            if (blackoutTimer !== undefined) window.clearTimeout(blackoutTimer);
            bench.close();
          },
        },
        {
          label: 'Continuar',
          primary: true,
          onClick: () => {
            if (blackoutTimer !== undefined) window.clearTimeout(blackoutTimer);
            bench.close(onSolved);
          },
        },
      ]);
      actions['Continuar'].classList.add('hidden');

      bench.setStatus(
        'La GalerÃ­a como la dejÃ³ el Consejo: cuatro lÃ¡mparas donde habÃ­a seis, todas igual de tenues. Antes de tocar nada, diagnostica la fila.',
      );
      render(false);
    },
  );
}

const GOAL_TEXT: Record<Phase, string> = {
  'predict-river': 'Â¿DÃ³nde crees que corre mÃ¡s rÃ­o? DespuÃ©s, mÃ­delo con Ohm.',
  measure: 'Mide la fila con Ohm, punto por punto.',
  'predict-removal': 'Â¿QuÃ© crees que les pasa a las otras si sacas una lÃ¡mpara?',
  remove: 'Saca una lÃ¡mpara y mira quÃ© le pasa a la fila.',
  restore: 'Devuelve el brillo a las SEIS lÃ¡mparas. Sin sacrificar ninguna.',
};

function phaseOf(state: ChainState): Phase {
  if (!state.predictions.river) return 'predict-river';
  if (!state.experiences.measuredSameRiver) return 'measure';
  if (!state.predictions.removal) return 'predict-removal';
  if (!state.experiences.removedLamp) return 'remove';
  return 'restore';
}

function appendPredictButtons<K extends string>(
  host: HTMLElement,
  options: { key: K; label: string }[],
  onPick: (key: K) => void,
): void {
  const row = document.createElement('div');
  row.className = 'bench-predict-row';
  for (const option of options) {
    const button = document.createElement('button');
    button.className = 'bench-predict-btn';
    button.textContent = option.label;
    button.addEventListener('click', () => onPick(option.key));
    row.appendChild(button);
  }
  host.appendChild(row);
}

function restoreFeedback(state: ChainState): string {
  const band = chainBand(state.push, state.lampCount);
  if (state.lampCount < CHAIN_MAX_LAMPS && band === 'bien') {
    return 'Las que estÃ¡n brillan bienâ€¦ pero faltan lÃ¡mparas en sus pedestales. La GalerÃ­a entera, no media.';
  }
  if (band === 'demasiado') {
    return 'Pocas lÃ¡mparas para tanto empuje: el rÃ­o las castiga, brillan al borde de quemarse. La fila quiere compaÃ±Ã­a.';
  }
  if (band === 'tenue' || band === 'casi-nada') {
    return 'Las seis encendidas, pero apenas. En una fila larga el freno se suma: hace falta mÃ¡s empuje.';
  }
  return 'La fila comparte un solo rÃ­o. Busca el brillo justo para las seis.';
}

function ohmStateFor(state: ChainState, blackedOut: boolean): 'inerte' | 'debil' | 'estable' | 'sobrecarga' {
  if (blackedOut) return 'inerte';
  const band = chainBand(state.push, state.lampCount);
  if (band === 'demasiado') return 'sobrecarga';
  if (band === 'bien') return 'estable';
  return 'debil';
}

function segmentLabel(segmentId: string, lampCount: number): string {
  if (segmentId === 'before') return 'Antes de la primera';
  if (segmentId === 'after') return 'DespuÃ©s de la Ãºltima';
  const left = Number(segmentId.replace('between-', ''));
  return `Entre lÃ¡mparas ${left} y ${Math.min(lampCount, left + 1)}`;
}

function formatRiver(rio: number): string {
  return Number.isInteger(rio) ? String(rio) : rio.toFixed(2);
}

function bandLabel(band: ChainBand): string {
  if (band === 'bien') return 'brillo justo';
  if (band === 'tenue') return 'tenue';
  if (band === 'casi-nada') return 'casi nada';
  return 'demasiado';
}

 succeeded in 1311ms:
# Spec Lote B+C â€” predicciÃ³n ligera y legibilidad (3 puzzles)

**Para el ejecutor (Codex).** Tres cambios SOLO DE VISTA (no toques modelos ni tests ni
rooms.ts). NO commitees. EspaÃ±ol neutro (tuteo). Imports `.ts`. PatrÃ³n compartido ya en el
repo: el panel de predicciÃ³n `.bench-predict` / `.bench-predict-q` / `.bench-predict-row` /
`.bench-predict-btn` (ver `src/puzzles/chain.ts`), y la legibilidad por estado del
Repartidor (`src/puzzles/distributor.ts`: "conforme / pide mÃ¡s / le sobra"). Tras terminar,
`npm run build` debe pasar. ReportÃ¡ quÃ© tocaste por archivo.

---

## B. `clock` â€” predicciÃ³n de apertura

**Problema:** el jugador ajusta con feedback instantÃ¡neo (bueno), pero nunca se compromete con
una expectativa antes (falta el "ajÃ¡" de predecirâ†’observar).

**Arreglo (vista `clock.ts`):**
- Al abrir, ANTES de poder tocar los controles (estanque/freno deshabilitados), mostrar un
  panel `.bench-predict` con la pregunta:
  `<b>Ohm:</b> Â«Antes de tocar nada: si agrandÃ¡s el Estanque, Â¿el tic se vuelve mÃ¡s rÃ¡pido o mÃ¡s lento?Â»`
  Botones: `MÃ¡s rÃ¡pido` (key `rapido`) Â· `MÃ¡s lento` (key `lento`). La verdad es **lento**
  (mÃ¡s estanque = perÃ­odo mayor = el tic tarda mÃ¡s).
- Al elegir, ocultar el panel, habilitar los controles, y mostrar en el status:
  - si eligiÃ³ `lento`: `<b>Farero:</b> Â«Bien pensado: mÃ¡s Estanque, mÃ¡s lento el tic. Ahora buscÃ¡ el tic justo (ritmo 4).Â»`
  - si eligiÃ³ `rapido`: `<b>Farero:</b> Â«Al revÃ©s de lo que parece: mÃ¡s Estanque tarda mÃ¡s en llenarse, asÃ­ que mÃ¡s lento. Comprobalo: buscÃ¡ el tic justo (ritmo 4).Â»`
- El resto del puzzle queda igual (feedback instantÃ¡neo, victoria con ritmo 4). La predicciÃ³n
  es estado local de la vista; NO toques `clockModel.ts`.

## C1. `singlestone` â€” rÃ­os a la vista (sin caja negra)

**Problema:** los rÃ­os de la red (izquierda) y de la piedra candidata (derecha) sÃ³lo se ven al
apretar "Pedirle a Ohm que mida" â†’ comparar a ciegas, ensayo-error. Y el contador es confuso.

**Arreglo (vista `singlestone.ts`; el modelo ya expone lo necesario):**
- Mostrar AMBOS rÃ­os EN VIVO, siempre, actualizados en cada cambio de red o de piedra (no sÃ³lo
  al medir). Calculalos en la vista con lo que el modelo ya exporta:
  - rÃ­o de la red = `SINGLE_STONE_PUSH / equivalentResistance(network)`
  - rÃ­o de la piedra = `SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone]`
  Mostralos en los readouts `[data-reading="left"]` y `[data-reading="right"]` (que hoy dicen
  "RÃ­o: â€”" hasta medir). Que dejen de decir "â€”": muestren el rÃ­o real al instante.
- AsÃ­ el jugador VE cuÃ¡ndo los dos rÃ­os coinciden antes de confirmar. MantenÃ© el botÃ³n
  "Pedirle a Ohm que mida" como confirmaciÃ³n (ya no es a ciegas): al apretarlo, si los rÃ­os son
  iguales registra la equivalencia (igual que hoy), si no, Ohm dice que los distingue (igual
  que hoy, mostrando los dos rÃ­os).
- ArreglÃ¡ el contador confuso: donde dice `Redes que Ohm no distinguiÃ³: N de 2`, ponÃ©
  `Equivalencias encontradas: N de 2`.
- No cambies la condiciÃ³n de victoria (2 equivalencias) ni `singlestoneModel.ts`.

## C2. `lighthouse` â€” condiciones legibles (la "brevedad" deja de ser opaca)

**Problema:** el ritmo objetivo (8) se entiende, pero por quÃ© la descarga es "breve" o "se
arrastra" es opaco (la duraciÃ³n del volcado nunca se muestra), y los 3 parÃ¡metros no tienen guÃ­a.

**Arreglo (vista `lighthouse.ts`; el modelo ya expone `rhythm`, `dischargePeriodMs`, `timing`,
`briefDischarge` vÃ­a `lighthouseReading`):**
- Mostrar DOS condiciones legibles, en vivo, al estilo del Repartidor (estado en palabra):
  1. Ritmo de carga: el valor `rhythm` y el objetivo 8, con estado `rÃ¡pido` (rhythm<8) /
     `justo` (=8) / `lento` (>8). Texto sugerido: `Ritmo de carga: <rhythm> Â· objetivo 8 Â· <estado>`.
  2. Volcado: la DURACIÃ“N real en segundos (`dischargePeriodMs / 1000`, p.ej. "0.2 s") con
     estado `de golpe` (si `briefDischarge`) / `se arrastra` (si no). Texto sugerido:
     `Volcado: <X.X> s Â· <de golpe | se arrastra>`.
  PonÃ© estos dos readouts visibles cerca de la referencia/controles (podÃ©s reemplazar o
  complementar el `.lighthouse-actual-label` actual). El de "volcado" hace deducible por quÃ©
  un freno de volcado alto "se arrastra".
- El estado `justo`/`de golpe` puede colorearse verde como en el Repartidor (clase `.ok`).
- No cambies la mecÃ¡nica, la victoria, las animaciones ni `lighthouseModel.ts`.

---

## VerificaciÃ³n
- `npm run build` (tsc + vite) sin errores.
- No tocaste modelos â†’ los tests existentes siguen pasando.

 succeeded in 1365ms:
import {
  sfxBridge,
  sfxClick,
  sfxDim,
  sfxHot,
  sfxOk,
  sfxTrunkFuse,
  sfxWin,
} from '../audio';
import { benchActions, openBench } from '../ui/bench';
import {
  fusible,
  gaugeSVG,
  ohmProbe,
  ohmWidgetHTML,
  piedraEl,
  PIEDRAS,
  setGauge,
  setOhmState,
} from './common';
import {
  DISTRIBUTOR_PUSHES,
  DISTRIBUTOR_STONES,
  DISTRIBUTOR_TARGETS,
  DISTRIBUTOR_TRUNK_TOLERANCE,
  createDistributorState,
  districtInGreenZone,
  distributorTrunkRiver,
  replaceDistributorFuse,
  setDistributorPush,
  setDistributorStone,
  type DistributorChange,
  type DistributorPush,
  type DistributorState,
} from './distributorModel';

export interface AbrirDistributorOptions {
  onSolved: () => void;
  onBurnedFuse: () => void;
  practica?: boolean;
}

const DISTRICTS = [
  { name: 'Forja', target: 4, className: 'forja' },
  { name: 'Campanario', target: 2, className: 'campanario' },
  { name: 'Biblioteca', target: 1, className: 'biblioteca' },
] as const;

const RESOLUTION =
  '<b>El Repartidor Â«cantaÂ»: un acorde de cobre.</b><br/>' +
  'El Castillo se enciende en cadena visible: forja, campanario, biblioteca, pasillos, almenas. ' +
  'Por las ventanas se ve la plaza de Ohmdal allÃ¡ abajo, y a los ciudadanos dÃ¡ndose vuelta hacia el Castillo.<br/><br/>' +
  '<b>InscripciÃ³n del mosaico:</b> Â«Lo que entra en el cruce, sale del cruce. ' +
  'Nada se pierde. Todo se reparte.Â»<br/><br/>' +
  '<b>Consejera:</b> Â«Cuarenta aÃ±os de inventariosâ€¦ Yo custodiaba un tesoro que no se gastaba por usarlo. ' +
  'El Ãºltimo sello es mÃ­o. Lo rompo yo.Â»<br/>' +
  '<b>Ohm:</b> Â«Registro histÃ³rico: Consejo actualizado. Tiempo transcurrido: una tarde.Â»<br/>' +
  '<b>Lumen:</b> Â«MÃ¡s rÃ¡pido que yo. No se lo digan.Â»';

const REPLACEMENT_COMMENTS = [
  '<b>Lumen:</b> Â«Un fusible para el CorazÃ³n del Castillo. Si me lo decÃ­an de aprendiz, me desmayo.Â»',
  '<b>Lumen:</b> Â«Otro mÃ¡s. La Consejera anota; yo repongo. Cada cual su liturgia.Â»',
  '<b>Lumen:</b> Â«â€¦Tengo mÃ¡s. No me miren asÃ­.Â»',
];

export function abrirDistributor(opts: AbrirDistributorOptions): void {
  const practica = opts.practica ?? false;
  openBench(
    'El Repartidor',
    'Reparto hacia objetivos distintos, con presupuesto de Tronco.',
    (bench) => {
      let state = createDistributorState();
      let solved = false;
      let burnedHandled = false;

      const sourceTray = document.createElement('div');
      sourceTray.className = 'bench-tray distributor-sources';
      sourceTray.innerHTML = '<span class="tray-label">Cristal de Empuje:</span>';
      const sourceButtons = new Map<DistributorPush, HTMLButtonElement>();
      for (const push of DISTRIBUTOR_PUSHES) {
        const button = document.createElement('button');
        button.className = 'fuente-opt distributor-source';
        button.textContent = `Empuje ${push}`;
        button.addEventListener('click', () => {
          if (solved || state.fuse.burned) return;
          sfxClick();
          applyChange(setDistributorPush(state, push));
          if (push === 16 && !state.solved) {
            bench.setStatus(
              '<b>Ohm:</b> Â«Empuje 16: a la forja le sobra brÃ­o, pero a la biblioteca no le das un hilo. ' +
              'Ni la piedra que mÃ¡s frena baja de RÃ­o 2.Â»',
            );
          }
        });
        sourceButtons.set(push, button);
        sourceTray.appendChild(button);
      }
      bench.root.appendChild(sourceTray);

      const stage = document.createElement('div');
      stage.className = 'bench-stage distributor-stage';
      stage.innerHTML = `
        <div class="distributor-network">
          <div class="distributor-crystal">Empuje <span>4</span></div>
          <div class="distributor-trunk-line">
            <div class="distributor-trunk live"></div>
            <span class="distributor-trunk-tag">Tronco</span>
          </div>
          <div class="distributor-crossing">Cruce</div>
          <div class="distributor-crossing-sum"></div>
          <div class="distributor-districts"></div>
        </div>
        <aside class="distributor-instruments">
          <div class="distributor-trunk-gauge">
            ${gaugeSVG(4, 4, 24)}
            <div class="distributor-gauge-label">aguja del Tronco Â· tolerancia 8</div>
            <div class="distributor-trunk-reading">Tronco: 0</div>
          </div>
          ${ohmWidgetHTML()}
        </aside>`;
      bench.root.appendChild(stage);

      const districtsHost = stage.querySelector<HTMLElement>('.distributor-districts')!;
      const districtRefs: {
        card: HTMLElement;
        state: HTMLElement;
        detail: HTMLElement;
        stones: HTMLElement;
        gauge: HTMLElement;
      }[] = [];

      DISTRICTS.forEach((district, index) => {
        const card = document.createElement('section');
        card.className = `distributor-district ${district.className}`;
        card.innerHTML = `
          <div class="distributor-wire live"></div>
          <h3>${district.name}</h3>
          <div class="distributor-emblem" aria-label="${district.name}"></div>
          <div class="distributor-district-gauge">
            ${gaugeSVG(district.target, 0.12, 8)}
          </div>
          <div class="distributor-district-state">â€”</div>
          <div class="distributor-district-detail">pide RÃ­o ${district.target} Â· llega RÃ­o 0</div>
          <div class="distributor-stones" aria-label="Piedras de Freno"></div>`;
        const stones = card.querySelector<HTMLElement>('.distributor-stones')!;
        for (const stone of DISTRIBUTOR_STONES) {
          const element = piedraEl(stone);
          element.setAttribute('role', 'button');
          element.setAttribute('tabindex', '0');
          const choose = () => {
            if (solved || state.fuse.burned) return;
            sfxBridge();
            applyChange(setDistributorStone(state, index, stone));
          };
          element.addEventListener('click', choose);
          element.addEventListener('keydown', (event) => {
            if (event.key === 'Enter' || event.key === ' ') {
              event.preventDefault();
              choose();
            }
          });
          stones.appendChild(element);
        }
        districtsHost.appendChild(card);
        districtRefs.push({
          card,
          state: card.querySelector<HTMLElement>('.distributor-district-state')!,
          detail: card.querySelector<HTMLElement>('.distributor-district-detail')!,
          stones,
          gauge: card.querySelector<HTMLElement>('.distributor-district-gauge')!,
        });
      });

      const fuse = fusible(DISTRIBUTOR_TRUNK_TOLERANCE, 24);
      fuse.element.classList.add('distributor-fuse');
      bench.root.appendChild(fuse.element);

      const probe = ohmProbe(
        [
          { id: 'trunk', label: 'Tronco' },
          { id: 'district-0', label: 'Forja' },
          { id: 'district-1', label: 'Campanario' },
          { id: 'district-2', label: 'Biblioteca' },
        ],
        (id) => {
          if (state.fuse.burned) return 'Sin rÃ­o. El fusible reventÃ³: camino cortado.';
          const river =
            id === 'trunk'
              ? distributorTrunkRiver(state)
              : state.districts[Number(id.replace('district-', ''))].river;
          return `RÃ­o: ${river}.`;
        },
        (reading, tramo) => bench.setStatus(`<b>${tramo.label}:</b> ${reading}`),
      );
      const probeHost = document.createElement('div');
      probeHost.className = 'distributor-probes';
      probeHost.appendChild(probe.element);
      bench.root.appendChild(probeHost);

      const actions = benchActions(bench.root, [
        { label: 'Alejarse', onClick: () => bench.close() },
        {
          label: 'Lumen repone el fusible',
          onClick: () => {
            state = replaceDistributorFuse(state);
            burnedHandled = false;
            fuse.reset();
            render();
            const comment =
              REPLACEMENT_COMMENTS[Math.min(state.replacements - 1, REPLACEMENT_COMMENTS.length - 1)];
            bench.setStatus(comment);
          },
        },
        {
          label: 'Continuar',
          primary: true,
          onClick: () => bench.close(opts.onSolved),
        },
      ]);
      actions['Lumen repone el fusible'].classList.add('hidden');
      actions['Continuar'].classList.add('hidden');

      function applyChange(change: DistributorChange): void {
        state = change.state;
        render();
        syncFuse(change);
        if (state.fuse.burned) return;

        if (state.solved) {
          sfxOk();
          if (practica) {
            bench.setStatus(
              state.alternativeSolution
                ? '<b>Ohm:</b> Â«Otra red. El mismo reparto.Â»'
                : '<b>El Repartidor Â«cantaÂ»: un acorde de cobre.</b>',
            );
            return;
          }
          if (!solved) {
            solved = true;
            sfxWin();
            const equivalence = state.alternativeSolution
              ? '<b>Ohm:</b> Â«Otra red. El mismo reparto.Â»<br/><br/>'
              : '';
            bench.setStatus(equivalence + RESOLUTION);
            actions['Alejarse'].classList.add('hidden');
            actions['Continuar'].classList.remove('hidden');
            render();
          }
          return;
        }

        if (change.fuseResult === 'warning') {
          sfxHot();
          bench.setStatus('<b>El fusible mayor avisa, vibra.</b>');
          return;
        }

        sfxDim();
        bench.setStatus(districtFeedback(state));
      }

      function syncFuse(change: DistributorChange): void {
        const visualResult = fuse.setValue(distributorTrunkRiver(state));
        if (
          change.fuseResult === 'burned' &&
          visualResult === 'burned' &&
          !burnedHandled
        ) {
          handleBurn();
        }
      }

      function handleBurn(): void {
        burnedHandled = true;
        sfxTrunkFuse();
        setOhmState(stage, 'sobrecarga');
        stage.classList.add('burned');
        actions['Lumen repone el fusible'].classList.remove('hidden');
        opts.onBurnedFuse();
        bench.setStatus(
          '<b>El fusible mayor se inmola.</b> Todo queda a oscuras. ' +
          'Lumen tiene repuestos. Siempre tiene repuestos.',
        );
        render();
      }

      function render(): void {
        const trunk = distributorTrunkRiver(state);
        const blackedOut = state.fuse.burned;
        stage.classList.toggle('burned', blackedOut);
        stage.querySelector<HTMLElement>('.distributor-crystal span')!.textContent =
          String(state.push);
        stage.querySelector('.distributor-trunk')?.classList.toggle('live', !blackedOut);
        stage.querySelector<HTMLElement>('.distributor-trunk-reading')!.textContent =
          `Tronco: ${blackedOut ? 0 : trunk}`;
        setGauge(stage.querySelector<HTMLElement>('.distributor-trunk-gauge')!, blackedOut ? 0 : trunk, 24);

        // La cuenta del Cruce: el Tronco lleva la suma de lo que toma cada distrito.
        const crossingSum = stage.querySelector<HTMLElement>('.distributor-crossing-sum')!;
        if (blackedOut) {
          crossingSum.classList.remove('over');
          crossingSum.innerHTML = '<span class="eq-hint">el fusible cortÃ³ el Tronco</span>';
        } else {
          const parts = state.districts.map((district) => district.river).join(' + ');
          crossingSum.textContent = `Tronco ${trunk} = ${parts}`;
          crossingSum.classList.toggle('over', trunk > DISTRIBUTOR_TRUNK_TOLERANCE);
        }

        setOhmState(
          stage,
          blackedOut
            ? 'inerte'
            : trunk > DISTRIBUTOR_TRUNK_TOLERANCE
              ? 'sobrecarga'
              : state.solved
                ? 'estable'
                : 'debil',
        );

        sourceButtons.forEach((button, push) => {
          button.classList.toggle('selected', push === state.push);
          button.disabled = solved || blackedOut;
        });

        districtRefs.forEach((refs, index) => {
          const district = state.districts[index];
          const river = blackedOut ? 0 : district.river;
          refs.card.classList.toggle('green', !blackedOut && districtInGreenZone(state, index));
          refs.card.classList.toggle('wrong', !blackedOut && !districtInGreenZone(state, index));
          refs.card.querySelector('.distributor-wire')?.classList.toggle('live', !blackedOut);
          const target = DISTRIBUTOR_TARGETS[index];
          const conforme = !blackedOut && district.river === target;
          refs.state.textContent = blackedOut
            ? 'â€”'
            : conforme
              ? 'conforme'
              : district.river < target
                ? 'pide mÃ¡s rÃ­o'
                : 'le sobra rÃ­o';
          refs.state.classList.toggle('ok', conforme);
          refs.state.classList.toggle('off', !blackedOut && !conforme);
          refs.detail.textContent = `pide RÃ­o ${target} Â· llega RÃ­o ${river}`;
          setGauge(refs.gauge, river, 8);
          refs.stones.querySelectorAll<HTMLElement>('.piedra').forEach((stone) => {
            const selected = stone.dataset.key === district.stone;
            stone.classList.toggle('selected', selected);
            stone.setAttribute('aria-disabled', String(solved || blackedOut));
            stone.title = `${PIEDRAS[stone.dataset.key!].nombre}: ${PIEDRAS[stone.dataset.key!].valor}`;
          });
        });

        probe.element.querySelectorAll('button').forEach((button) => {
          button.disabled = solved;
        });
      }

      bench.setStatus(
        'La forja pide RÃ­o 4; el campanario, RÃ­o 2; la biblioteca, RÃ­o 1. ' +
        'El Tronco tolera hasta 8.',
      );
      render();
    },
  );
}

function districtFeedback(state: DistributorState): string {
  const feedback: string[] = [];
  if (!districtInGreenZone(state, 0)) {
    feedback.push('<b>Forja:</b> la forja Â«toseÂ» hollÃ­n.');
  }
  if (!districtInGreenZone(state, 1)) {
    feedback.push('<b>Campanario:</b> da una nota desafinada.');
  }
  if (!districtInGreenZone(state, 2)) {
    feedback.push(
      state.districts[2].river > DISTRIBUTOR_TARGETS[2]
        ? '<b>Biblioteca:</b> le llega rÃ­o de mÃ¡sâ€¦ y aplaude. Le gusta la penumbra.'
        : '<b>Biblioteca:</b> su emblema parpadea dÃ©bil.',
    );
  }
  return feedback.join('<br/>');
}

2026-06-22T22:46:50.463855Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3.4 seconds
Output:
rg : El término 'rg' no se reconoce como nombre de un cmdlet, función, archivo de script o programa ejecutable. 
Compruebe si escribió correctamente el nombre o, si incluyó una ruta de acceso, compruebe que dicha ruta es correcta e 
inténtelo de nuevo.
En línea: 2 Carácter: 1
+ rg --files src | rg "(clock|singlestone|lighthouse|styles\.css)$"
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

 exited 1 in 3428ms:
rg : El término 'rg' no se reconoce como nombre de un cmdlet, función, archivo de script o programa ejecutable. 
Compruebe si escribió correctamente el nombre o, si incluyó una ruta de acceso, compruebe que dicha ruta es correcta e 
inténtelo de nuevo.
En línea: 2 Carácter: 1
+ rg --files src | rg "(clock|singlestone|lighthouse|styles\.css)$"
+ ~~
    + CategoryInfo          : ObjectNotFound: (rg:String) [], CommandNotFoundException
    + FullyQualifiedErrorId : CommandNotFoundException
 

codex
`rg` no está disponible en este entorno, así que sigo con PowerShell nativo. Ya tengo la spec y los patrones; ahora leo las tres vistas y sus modelos para ajustar solo lo que la vista ya puede calcular/exportar.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/clock.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/clockModel.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/lighthouse.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/singlestone.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/singlestoneModel.ts'" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/puzzles/lighthouseModel.ts'" in C:\YO\Proyectos\Roxana claude
 succeeded in 812ms:
import { sfxBridge, sfxClick, sfxWin } from '../audio';
import { benchActions, openBench } from '../ui/bench';
import { createSimTick, piedraEl, PIEDRAS, setTankLevel, tankSVG } from './common';
import {
  advanceClock,
  clockReading,
  configureClock,
  createClockState,
  type ClockBrake,
  type ClockTank,
  type ClockTiming,
} from './clockModel';

const TANKS: { value: ClockTank; label: string }[] = [
  { value: 1, label: 'chico' },
  { value: 2, label: 'mediano' },
  { value: 4, label: 'grande' },
];
const BRAKES: { key: string; value: ClockBrake }[] = [
  { key: 'marron', value: 1 },
  { key: 'roja', value: 2 },
  { key: 'amarilla', value: 4 },
  { key: 'gris', value: 8 },
];

const FAST_DIALOGUE = '<b>Farero:</b> Â«Ese reloj tomÃ³ cafÃ©. MÃ¡s despacio.Â»';
const SLOW_DIALOGUE =
  '<b>Farero:</b> Â«Ese reloj estÃ¡ por jubilarse. Un poco mÃ¡s de brÃ­o.Â»';
const JUST_DIALOGUE =
  '<b>Ohm:</b> Â«Otro estanque. El mismo tiempo.Â»<br/>' +
  '<b>Farero:</b> Â«Ese. Ese es el tic. Lo reconozco aunque me despierten a las tres.Â»';

export function abrirClock(onSolved: () => void, practica = false): void {
  openBench(
    'El Reloj de Ohmdal',
    'DevuÃ©lvale un tic justo al pueblo. Ritmo objetivo: 4.',
    (bench) => {
      let state = createClockState();
      let solved = false;

      const stage = document.createElement('div');
      stage.className = 'bench-stage clock-stage';
      stage.innerHTML = `
        <div class="clock-machine">
          <div class="clock-source">fuente</div>
          <div class="clock-channel">
            <span class="clock-flow"></span>
            <div class="clock-stone-slot"></div>
          </div>
          <div class="clock-tank" data-tank="1">
            <span class="clock-threshold">umbral de volcado</span>
            ${tankSVG()}
          </div>
          <div class="clock-pendulum" aria-label="PÃ©ndulo del Reloj">
            <div class="clock-face">
              <span class="clock-hand"></span>
            </div>
            <div class="clock-rod"></div>
            <div class="clock-weight"></div>
            <span>pÃ©ndulo</span>
          </div>
        </div>
        <div class="clock-reference">
          <div>
            <strong>Referencia</strong>
            <span>tic objetivo Â· ritmo 4</span>
          </div>
          <div class="clock-reference-pulse"><span></span></div>
          <div class="clock-actual">
            <strong>Reloj</strong>
            <span class="clock-actual-label">adelanta</span>
          </div>
          <div class="clock-actual-pulse"><span></span></div>
        </div>
        <div class="clock-controls">
          <section>
            <h3>Estanque</h3>
            <div class="clock-tanks"></div>
          </section>
          <section>
            <h3>Freno del canal</h3>
            <div class="clock-brakes"></div>
          </section>
        </div>`;
      bench.root.appendChild(stage);

      const tankButtons: HTMLButtonElement[] = [];
      const brakeButtons: HTMLButtonElement[] = [];
      addTankChoices();
      addBrakeChoices();

      const actions = benchActions(bench.root, [
        { label: 'Alejarse', onClick: () => bench.close() },
        {
          label: 'Continuar',
          primary: true,
          onClick: () => bench.close(onSolved),
        },
      ]);
      actions['Continuar'].classList.add('hidden');

      const tick = createSimTick((dtMs) => {
        const previousStep = state.pendulumStep;
        state = advanceClock(state, dtMs);
        if (state.pendulumStep > previousStep) sfxBridge();
        render();
      });
      bench.onClose(tick.stop);

      function addTankChoices(): void {
        const host = stage.querySelector<HTMLElement>('.clock-tanks')!;
        for (const tank of TANKS) {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'clock-choice';
          button.dataset.tank = String(tank.value);
          button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
          button.addEventListener('click', () => {
            if (solved) return;
            sfxClick();
            state = configureClock(state, tank.value, state.brake);
            render();
            evaluateConfiguration();
          });
          host.appendChild(button);
          tankButtons.push(button);
        }
      }

      function addBrakeChoices(): void {
        const host = stage.querySelector<HTMLElement>('.clock-brakes')!;
        for (const brake of BRAKES) {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'clock-brake';
          button.dataset.brake = String(brake.value);
          button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
          button.appendChild(piedraEl(brake.key));
          button.addEventListener('click', () => {
            if (solved) return;
            sfxClick();
            state = configureClock(state, state.tank, brake.value);
            render();
            evaluateConfiguration();
          });
          host.appendChild(button);
          brakeButtons.push(button);
        }
      }

      function evaluateConfiguration(): void {
        const timing = clockReading(state).timing;
        bench.setStatus(feedbackFor(timing));
        if (timing !== 'just' || practica || solved) return;

        solved = true;
        sfxWin();
        tankButtons.forEach((button) => {
          button.disabled = true;
        });
        brakeButtons.forEach((button) => {
          button.disabled = true;
        });
        actions['Alejarse'].classList.add('hidden');
        actions['Continuar'].classList.remove('hidden');
        bench.setStatus(
          `${JUST_DIALOGUE}<br/><br/><b>Toda la plaza oye el tic.</b> ` +
          'La voz que recuperÃ³ con la campana ahora tiene pulso.',
        );
      }

      function feedbackFor(timing: ClockTiming): string {
        if (timing === 'fast') return FAST_DIALOGUE;
        if (timing === 'slow') return SLOW_DIALOGUE;
        return JUST_DIALOGUE;
      }

      function render(): void {
        const reading = clockReading(state);
        setTankLevel(stage, state.level);
        stage.dataset.timing = reading.timing;
        stage.style.setProperty('--clock-period', `${reading.periodMs}ms`);
        stage.querySelector<HTMLElement>('.clock-tank')!.dataset.tank = String(state.tank);
        stage.querySelector<HTMLElement>('.clock-actual-label')!.textContent =
          reading.timing === 'fast'
            ? 'adelanta'
            : reading.timing === 'slow'
              ? 'atrasa'
              : 'tic justo';

        const stoneSlot = stage.querySelector<HTMLElement>('.clock-stone-slot')!;
        const brake = BRAKES.find((candidate) => candidate.value === state.brake)!;
        stoneSlot.replaceChildren(piedraEl(brake.key));
        stoneSlot.querySelector('.piedra')?.classList.add('in-slot');

        const side = state.pendulumStep % 2 === 0 ? -28 : 28;
        stage.querySelector<HTMLElement>('.clock-rod')!.style.transform = `rotate(${side}deg)`;
        stage.querySelector<HTMLElement>('.clock-weight')!.style.transform =
          `translateX(${side * 1.25}px)`;
        stage.querySelector<HTMLElement>('.clock-hand')!.style.transform =
          `rotate(${state.pendulumStep * 30}deg)`;

        tankButtons.forEach((button) => {
          button.classList.toggle('selected', button.dataset.tank === String(state.tank));
        });
        brakeButtons.forEach((button) => {
          button.classList.toggle('selected', button.dataset.brake === String(state.brake));
        });
      }

      bench.setStatus(
        'El Estanque se llena y vuelca solo. Cambie el Estanque o el freno para ajustar el tic.',
      );
      render();
    },
  );
}

 succeeded in 840ms:
export type ClockTank = 1 | 2 | 4;
export type ClockBrake = 1 | 2 | 4 | 8;
export type ClockTiming = 'fast' | 'just' | 'slow';

export const CLOCK_UNIT_MS = 250;
export const CLOCK_TARGET_RHYTHM = 4;

export interface ClockState {
  tank: ClockTank;
  brake: ClockBrake;
  level: number;
  pendulumStep: number;
}

export interface ClockReading {
  rhythm: number;
  periodMs: number;
  timing: ClockTiming;
}

export function clockRhythm(tank: ClockTank, brake: ClockBrake): number {
  return tank * brake;
}

export function clockPeriodMs(tank: ClockTank, brake: ClockBrake): number {
  return CLOCK_UNIT_MS * clockRhythm(tank, brake);
}

export function clockTiming(tank: ClockTank, brake: ClockBrake): ClockTiming {
  const rhythm = clockRhythm(tank, brake);
  if (rhythm < CLOCK_TARGET_RHYTHM) return 'fast';
  if (rhythm > CLOCK_TARGET_RHYTHM) return 'slow';
  return 'just';
}

export function createClockState(
  tank: ClockTank = 1,
  brake: ClockBrake = 1,
): ClockState {
  return { tank, brake, level: 0, pendulumStep: 0 };
}

export function configureClock(
  state: ClockState,
  tank: ClockTank,
  brake: ClockBrake,
): ClockState {
  return { ...state, tank, brake, level: 0 };
}

export function advanceClock(state: ClockState, dtMs: number): ClockState {
  const elapsed = Number.isFinite(dtMs) ? Math.max(0, dtMs) : 0;
  if (elapsed === 0) return state;

  const progress = state.level + (elapsed / clockPeriodMs(state.tank, state.brake)) * 100;
  const ticks = Math.floor(progress / 100);
  return {
    ...state,
    level: progress % 100,
    pendulumStep: state.pendulumStep + ticks,
  };
}

export function clockReading(state: ClockState): ClockReading {
  return {
    rhythm: clockRhythm(state.tank, state.brake),
    periodMs: clockPeriodMs(state.tank, state.brake),
    timing: clockTiming(state.tank, state.brake),
  };
}

 succeeded in 902ms:
import { sfxClick, sfxOk, sfxWin } from '../audio';
import { benchActions, openBench } from '../ui/bench';
import {
  ohmWidgetHTML,
  piedraEl,
  PIEDRAS,
  setOhmState,
} from './common';
import {
  SINGLE_STONE_NETWORKS,
  SINGLE_STONE_PUSH,
  SINGLE_STONE_REQUIRED_MATCHES,
  compareSingleStone,
  createSingleStoneState,
  isSingleStoneSolved,
  recordSingleStoneMatch,
  type SingleStone,
  type SingleStoneNetwork,
  type SingleStoneNetworkId,
} from './singlestoneModel';

const STONES: SingleStone[] = ['marron', 'roja', 'amarilla', 'gris'];

const SOLVED_DIALOGUE =
  '<b>Edda:</b> Â«La red entera se esconde dentro de una piedra. Y si Ohm no la distingueâ€¦Â»<br/>' +
  '<b>Ohm:</b> Â«â€¦el rÃ­o tampoco. Una red es una piedra que todavÃ­a no terminaste de mirar.Â»<br/>' +
  '<b>Guardiana:</b> Â«Entonces el valle enteroâ€¦ es una piedra. Una sola piedra que aprendÃ­ a temer.Â»';

export function abrirSingleStone(onSolved: () => void, practica = false): void {
  openBench(
    'La Piedra Ãšnica',
    'Arme una red a la izquierda y encuentre la Ãºnica piedra que lleva el mismo rÃ­o a la derecha.',
    (bench) => {
      let networkId: SingleStoneNetworkId = SINGLE_STONE_NETWORKS[0].id;
      let candidateStone: SingleStone = 'gris';
      let state = createSingleStoneState();
      let solved = false;

      const stage = document.createElement('div');
      stage.className = 'bench-stage singlestone-stage';
      stage.innerHTML = `
        <div class="singlestone-workbench">
          <section class="singlestone-side singlestone-left">
            <h3>Red armable</h3>
            <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
            <div class="singlestone-network" aria-live="polite"></div>
            <div class="singlestone-reading" data-reading="left">RÃ­o: â€”</div>
          </section>
          <div class="singlestone-ohm">
            ${ohmWidgetHTML('Ohm Â· manantial')}
            <span>Empuje ${SINGLE_STONE_PUSH} a cada lado</span>
          </div>
          <section class="singlestone-side singlestone-right">
            <h3>Una piedra</h3>
            <div class="singlestone-slot"></div>
            <div class="singlestone-stones" aria-label="Piedras Ãºnicas posibles"></div>
            <div class="singlestone-reading" data-reading="right">RÃ­o: â€”</div>
          </section>
        </div>
        <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
        <div class="singlestone-progress" aria-label="Equivalencias encontradas"></div>`;
      bench.root.appendChild(stage);

      const networkButtons = new Map<SingleStoneNetworkId, HTMLButtonElement>();
      const networkChoices = stage.querySelector<HTMLElement>('.singlestone-network-choices')!;
      for (const network of SINGLE_STONE_NETWORKS) {
        const button = document.createElement('button');
        button.type = 'button';
        button.className = 'singlestone-network-choice';
        button.textContent = network.label;
        button.addEventListener('click', () => selectNetwork(network.id));
        networkChoices.appendChild(button);
        networkButtons.set(network.id, button);
      }

      const stoneButtons = new Map<SingleStone, HTMLButtonElement>();
      const stonesHost = stage.querySelector<HTMLElement>('.singlestone-stones')!;
      for (const stone of STONES) {
        const button = document.createElement('button');
        button.type = 'button';
        button.className = 'singlestone-stone';
        button.setAttribute('aria-label', PIEDRAS[stone].nombre);
        button.appendChild(piedraEl(stone));
        button.addEventListener('click', () => selectStone(stone));
        stonesHost.appendChild(button);
        stoneButtons.set(stone, button);
      }

      const compareButton = stage.querySelector<HTMLButtonElement>('.singlestone-compare')!;
      compareButton.addEventListener('click', compare);

      const actions = benchActions(bench.root, [
        { label: 'Alejarse', onClick: () => bench.close() },
        {
          label: 'Continuar',
          primary: true,
          onClick: () => bench.close(onSolved),
        },
      ]);
      actions['Continuar'].classList.add('hidden');

      function selectNetwork(nextNetworkId: SingleStoneNetworkId): void {
        if (solved) return;
        sfxClick();
        networkId = nextNetworkId;
        clearReadings();
        render();
        bench.setStatus('Red elegida. Falta encontrar su Piedra Ãšnica.');
      }

      function selectStone(stone: SingleStone): void {
        if (solved) return;
        sfxClick();
        candidateStone = stone;
        clearReadings();
        render();
        bench.setStatus('Piedra colocada. Ohm puede comparar los dos rÃ­os.');
      }

      function compare(): void {
        if (solved) return;
        sfxClick();
        const comparison = compareSingleStone(networkId, candidateStone);
        setReading('left', comparison.leftRiver);
        setReading('right', comparison.rightRiver);

        if (comparison.distinguishes) {
          setOhmState(stage, 'debil');
          bench.setStatus(
            `<b>Ohm:</b> Â«Izquierda: rÃ­o ${formatNumber(comparison.leftRiver)}. Derecha: rÃ­o ${formatNumber(comparison.rightRiver)}. Distingo.Â»`,
          );
          return;
        }

        sfxOk();
        state = recordSingleStoneMatch(state, comparison);
        setOhmState(stage, 'estable');
        render();
        const matchingDialogue =
          `<b>Ohm:</b> Â«Izquierda: rÃ­o ${formatNumber(comparison.leftRiver)}. ` +
          `Derecha: rÃ­o ${formatNumber(comparison.rightRiver)}. No distingo. Son la misma piedra.Â»`;

        if (!practica && isSingleStoneSolved(state)) {
          solved = true;
          sfxWin();
          disableWorkbench();
          actions['Alejarse'].classList.add('hidden');
          actions['Continuar'].classList.remove('hidden');
          bench.setStatus(`${matchingDialogue}<br/><br/>${SOLVED_DIALOGUE}`);
          return;
        }

        bench.setStatus(matchingDialogue);
      }

      function clearReadings(): void {
        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'RÃ­o: â€”';
        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'RÃ­o: â€”';
        setOhmState(stage, 'inerte');
      }

      function setReading(side: 'left' | 'right', value: number): void {
        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
          `RÃ­o: ${formatNumber(value)}`;
      }

      function disableWorkbench(): void {
        stage.querySelectorAll<HTMLButtonElement>('button').forEach((button) => {
          button.disabled = true;
        });
      }

      function render(): void {
        const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
        renderNetwork(
          stage.querySelector<HTMLElement>('.singlestone-network')!,
          network,
        );
        const slot = stage.querySelector<HTMLElement>('.singlestone-slot')!;
        slot.replaceChildren(piedraEl(candidateStone));
        slot.querySelector('.piedra')?.classList.add('in-slot');

        for (const [id, button] of networkButtons) {
          button.classList.toggle('selected', id === networkId);
          button.classList.toggle('matched', state.matchedNetworkIds.includes(id));
        }
        for (const [stone, button] of stoneButtons) {
          button.classList.toggle('selected', stone === candidateStone);
        }
        renderProgress();
      }

      function renderProgress(): void {
        const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
        progress.textContent =
          `Redes que Ohm no distinguiÃ³: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
      }

      bench.setStatus(
        '<b>Guardiana:</b> Â«Este mural lleva aquÃ­ mÃ¡s que yo. Nunca lo entendÃ­. Una maraÃ±aâ€¦ igual a una piedra. Â¿Igual cÃ³mo?Â»<br/>' +
        '<b>Ohm:</b> Â«Demostrable. TrÃ¡iganme una red. Yo decido si la distingo.Â»',
      );
      render();
      clearReadings();
    },
  );
}

function renderNetwork(host: HTMLElement, network: SingleStoneNetwork): void {
  host.innerHTML = `
    <div class="singlestone-source">manantial</div>
    <div class="singlestone-wire"></div>
    <div class="singlestone-series"></div>
    <div class="singlestone-parallel">
      <div class="singlestone-branch" data-branch="one"></div>
      <div class="singlestone-branch" data-branch="two"></div>
    </div>
    <div class="singlestone-return">vuelta</div>`;

  const seriesHost = host.querySelector<HTMLElement>('.singlestone-series')!;
  if (network.seriesStone) {
    seriesHost.appendChild(piedraEl(network.seriesStone));
    seriesHost.querySelector('.piedra')?.classList.add('in-slot');
  } else {
    seriesHost.remove();
  }

  host.querySelectorAll<HTMLElement>('.singlestone-branch').forEach((branch) => {
    branch.appendChild(piedraEl(network.parallelStone));
    branch.querySelector('.piedra')?.classList.add('in-slot');
  });
}

function formatNumber(value: number): string {
  return Number.isInteger(value)
    ? String(value)
    : value.toFixed(2).replace(/0+$/, '').replace(/\.$/, '');
}

 succeeded in 960ms:
import { sfxBridge, sfxClick, sfxWin } from '../audio';
import { benchActions, openBench } from '../ui/bench';
import { createSimTick, piedraEl, PIEDRAS, setTankLevel, tankSVG } from './common';
import {
  advanceLighthouse,
  configureLighthouse,
  createLighthouseState,
  lighthouseReading,
  type LighthouseBrake,
  type LighthouseTank,
  type LighthouseTiming,
} from './lighthouseModel';

const TANKS: { value: LighthouseTank; label: string }[] = [
  { value: 1, label: 'chico' },
  { value: 2, label: 'mediano' },
  { value: 4, label: 'grande' },
];
const BRAKES: { key: string; value: LighthouseBrake }[] = [
  { key: 'marron', value: 1 },
  { key: 'roja', value: 2 },
  { key: 'amarilla', value: 4 },
  { key: 'gris', value: 8 },
];

const FAST_DIALOGUE =
  '<b>Farero:</b> Â«Tartamudea. Ese no avisa: balbucea. MÃ¡s lento, que el barco tiene que verlo venir.Â»';
const SLOW_DIALOGUE =
  '<b>Farero:</b> Â«Casi se queda quieto. Un faro dormido no salva a nadie. Un poco mÃ¡s de pulso.Â»';
const DRAGGED_DUMP_DIALOGUE =
  '<b>Farero:</b> Â«El tiempo es ese, lo reconozcoâ€¦ pero el destello se arrastra como despedida. Tiene que volcar de golpe: la salida, casi libre.Â»';
const JUST_DIALOGUE =
  '<b>Farero:</b> Â«Ese. Ese es. â€¦Cuarenta aÃ±os afinando el oÃ­do para esta noche.Â» ' +
  '<em>(se le llenan los ojos)</em><br/><br/>' +
  '<b>Ohm</b> gira hacia la luz; su pecho parpadea al mismo ritmo una vez.<br/>' +
  '<em>(silencio)</em><br/><br/>' +
  '<b>Edda:</b> Â«Â¿Ohmâ€¦? Tu pecho. Late igual que el Faro.Â»<br/>' +
  '<b>Ohm:</b> Â«Dato registrado. Sin explicaciÃ³n disponible. â€¦TodavÃ­a.Â»';

export function abrirLighthouse(onSolved: () => void, practica = false): void {
  openBench(
    'El latido del Faro',
    'Llene despacio por un camino y vuelque de golpe por el otro. Ritmo objetivo: 8.',
    (bench) => {
      let state = createLighthouseState();
      let solved = false;

      const stage = document.createElement('div');
      stage.className = 'bench-stage lighthouse-stage';
      stage.innerHTML = `
        <div class="lighthouse-machine">
          <div class="lighthouse-source">fuente</div>
          <div class="lighthouse-channel lighthouse-charge-channel">
            <span class="lighthouse-flow"></span>
            <span class="lighthouse-path-label">canal de carga</span>
            <div class="lighthouse-charge-slot"></div>
          </div>
          <div class="lighthouse-tank" data-tank="1">
            <span class="lighthouse-threshold">umbral de volcado</span>
            ${tankSVG()}
          </div>
          <div class="lighthouse-channel lighthouse-discharge-channel">
            <span class="lighthouse-flow"></span>
            <span class="lighthouse-path-label">camino de volcado</span>
            <div class="lighthouse-discharge-slot"></div>
          </div>
          <div class="lighthouse-lens" aria-label="Lente del Faro">
            <span class="lighthouse-lens-glow"></span>
            <strong>lente</strong>
          </div>
        </div>
        <div class="lighthouse-lake">lago negro</div>
        <div class="lighthouse-reference">
          <div>
            <strong>Referencia</strong>
            <span>latido objetivo Â· ritmo 8 Â· ~2 s</span>
          </div>
          <div class="lighthouse-reference-pulse"><span></span></div>
          <div>
            <strong>Faro</strong>
            <span class="lighthouse-actual-label">demasiado rÃ¡pido</span>
          </div>
          <div class="lighthouse-actual-pulse"><span></span></div>
        </div>
        <div class="lighthouse-controls">
          <section>
            <h3>Estanque</h3>
            <div class="lighthouse-tanks"></div>
          </section>
          <section>
            <h3>Freno de carga</h3>
            <div class="lighthouse-charge-brakes"></div>
          </section>
          <section>
            <h3>Freno de volcado</h3>
            <div class="lighthouse-discharge-brakes"></div>
          </section>
        </div>`;
      bench.root.appendChild(stage);

      const tankButtons: HTMLButtonElement[] = [];
      const chargeButtons: HTMLButtonElement[] = [];
      const dischargeButtons: HTMLButtonElement[] = [];
      addTankChoices();
      addBrakeChoices('charge');
      addBrakeChoices('discharge');

      const actions = benchActions(bench.root, [
        { label: 'Alejarse', onClick: () => bench.close() },
        {
          label: 'Continuar',
          primary: true,
          onClick: () => bench.close(onSolved),
        },
      ]);
      actions['Continuar'].classList.add('hidden');

      const tick = createSimTick((dtMs) => {
        if (solved) return;
        const previousFlash = state.flashCount;
        state = advanceLighthouse(state, dtMs);
        if (state.flashCount > previousFlash) sfxBridge();
        render();
      });
      bench.onClose(tick.stop);

      function addTankChoices(): void {
        const host = stage.querySelector<HTMLElement>('.lighthouse-tanks')!;
        for (const tank of TANKS) {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'lighthouse-choice';
          button.dataset.tank = String(tank.value);
          button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
          button.addEventListener('click', () => {
            if (solved) return;
            sfxClick();
            state = configureLighthouse(
              state,
              tank.value,
              state.chargeBrake,
              state.dischargeBrake,
            );
            render();
            evaluateConfiguration();
          });
          host.appendChild(button);
          tankButtons.push(button);
        }
      }

      function addBrakeChoices(path: 'charge' | 'discharge'): void {
        const host = stage.querySelector<HTMLElement>(
          path === 'charge'
            ? '.lighthouse-charge-brakes'
            : '.lighthouse-discharge-brakes',
        )!;
        const buttons = path === 'charge' ? chargeButtons : dischargeButtons;
        for (const brake of BRAKES) {
          const button = document.createElement('button');
          button.type = 'button';
          button.className = 'lighthouse-brake';
          button.dataset.brake = String(brake.value);
          button.setAttribute(
            'aria-label',
            `${PIEDRAS[brake.key].nombre}, freno ${brake.value} de ${path === 'charge' ? 'carga' : 'volcado'}`,
          );
          button.appendChild(piedraEl(brake.key));
          button.addEventListener('click', () => {
            if (solved) return;
            sfxClick();
            state = configureLighthouse(
              state,
              state.tank,
              path === 'charge' ? brake.value : state.chargeBrake,
              path === 'discharge' ? brake.value : state.dischargeBrake,
            );
            render();
            evaluateConfiguration();
          });
          host.appendChild(button);
          buttons.push(button);
        }
      }

      function evaluateConfiguration(): void {
        const reading = lighthouseReading(state);
        if (!reading.valid || practica || solved) {
          bench.setStatus(feedbackFor(reading.timing, reading.briefDischarge));
          return;
        }

        solved = true;
        tick.stop();
        sfxWin();
        stage.classList.add('restored');
        [...tankButtons, ...chargeButtons, ...dischargeButtons].forEach((button) => {
          button.disabled = true;
        });
        actions['Alejarse'].classList.add('hidden');
        actions['Continuar'].classList.remove('hidden');
        bench.setStatus(
          '<b>El lago entero parpadea con la lente.</b><br/><br/>' + JUST_DIALOGUE,
        );
      }

      function feedbackFor(timing: LighthouseTiming, briefDischarge: boolean): string {
        if (timing === 'fast') return FAST_DIALOGUE;
        if (timing === 'slow') return SLOW_DIALOGUE;
        if (!briefDischarge) return DRAGGED_DUMP_DIALOGUE;
        return 'Ritmo de carga 8 y volcado breve. El Faro sostiene el latido restaurado.';
      }

      function render(): void {
        const reading = lighthouseReading(state);
        setTankLevel(stage, state.level);
        stage.dataset.timing = reading.timing;
        stage.dataset.phase = state.phase;
        stage.dataset.discharge = reading.briefDischarge ? 'brief' : 'dragged';
        stage.style.setProperty('--lighthouse-period', `${reading.chargePeriodMs}ms`);
        stage.querySelector<HTMLElement>('.lighthouse-tank')!.dataset.tank = String(state.tank);
        stage.querySelector<HTMLElement>('.lighthouse-actual-label')!.textContent =
          reading.timing === 'fast'
            ? 'demasiado rÃ¡pido'
            : reading.timing === 'slow'
              ? 'demasiado lento'
              : reading.briefDischarge
                ? 'latido justo'
                : 'volcado lento';

        renderStone('.lighthouse-charge-slot', state.chargeBrake);
        renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
        tankButtons.forEach((button) => {
          button.classList.toggle('selected', button.dataset.tank === String(state.tank));
        });
        chargeButtons.forEach((button) => {
          button.classList.toggle(
            'selected',
            button.dataset.brake === String(state.chargeBrake),
          );
        });
        dischargeButtons.forEach((button) => {
          button.classList.toggle(
            'selected',
            button.dataset.brake === String(state.dischargeBrake),
          );
        });
      }

      function renderStone(selector: string, value: LighthouseBrake): void {
        const slot = stage.querySelector<HTMLElement>(selector)!;
        const brake = BRAKES.find((candidate) => candidate.value === value)!;
        slot.replaceChildren(piedraEl(brake.key));
        slot.querySelector('.piedra')?.classList.add('in-slot');
      }

      bench.setStatus(
        'Ajuste los dos caminos. El Farero responde en cuanto cambia el ritmo.',
      );
      render();
    },
  );
}

 succeeded in 944ms:
export const SINGLE_STONE_PUSH = 8;
export const SINGLE_STONE_REQUIRED_MATCHES = 2;

export type SingleStone = 'marron' | 'roja' | 'amarilla' | 'gris';

export const SINGLE_STONE_VALUES: Record<SingleStone, number> = {
  marron: 1,
  roja: 2,
  amarilla: 4,
  gris: 8,
};

export type SingleStoneNetworkId =
  | 'parallel-red'
  | 'parallel-yellow'
  | 'parallel-gray'
  | 'brown-series-parallel-red'
  | 'red-series-parallel-yellow';

export interface SingleStoneNetwork {
  id: SingleStoneNetworkId;
  label: string;
  seriesStone?: SingleStone;
  parallelStone: SingleStone;
  equivalentStone: SingleStone;
}

export const SINGLE_STONE_NETWORKS: readonly SingleStoneNetwork[] = [
  {
    id: 'parallel-red',
    label: 'Dos rojas en ramales',
    parallelStone: 'roja',
    equivalentStone: 'marron',
  },
  {
    id: 'parallel-yellow',
    label: 'Dos amarillas en ramales',
    parallelStone: 'amarilla',
    equivalentStone: 'roja',
  },
  {
    id: 'parallel-gray',
    label: 'Dos grises en ramales',
    parallelStone: 'gris',
    equivalentStone: 'amarilla',
  },
  {
    id: 'brown-series-parallel-red',
    label: 'MarrÃ³n en fila con dos rojas en ramales',
    seriesStone: 'marron',
    parallelStone: 'roja',
    equivalentStone: 'roja',
  },
  {
    id: 'red-series-parallel-yellow',
    label: 'Roja en fila con dos amarillas en ramales',
    seriesStone: 'roja',
    parallelStone: 'amarilla',
    equivalentStone: 'amarilla',
  },
];

export interface SingleStoneComparison {
  networkId: SingleStoneNetworkId;
  candidateStone: SingleStone;
  leftResistance: number;
  rightResistance: number;
  leftRiver: number;
  rightRiver: number;
  distinguishes: boolean;
}

export interface SingleStoneState {
  matchedNetworkIds: SingleStoneNetworkId[];
}

export function parallelEqualResistance(stone: SingleStone): number {
  return SINGLE_STONE_VALUES[stone] / 2;
}

export function seriesResistance(...resistances: number[]): number {
  return resistances.reduce((total, resistance) => total + resistance, 0);
}

export function equivalentResistance(network: SingleStoneNetwork): number {
  const parallel = parallelEqualResistance(network.parallelStone);
  return network.seriesStone
    ? seriesResistance(SINGLE_STONE_VALUES[network.seriesStone], parallel)
    : parallel;
}

export function getSingleStoneNetwork(id: SingleStoneNetworkId): SingleStoneNetwork {
  const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === id);
  if (!network) throw new Error(`Red de Piedra Ãšnica desconocida: ${id}`);
  return network;
}

export function compareSingleStone(
  networkId: SingleStoneNetworkId,
  candidateStone: SingleStone,
): SingleStoneComparison {
  const network = getSingleStoneNetwork(networkId);
  const leftResistance = equivalentResistance(network);
  const rightResistance = SINGLE_STONE_VALUES[candidateStone];
  const leftRiver = SINGLE_STONE_PUSH / leftResistance;
  const rightRiver = SINGLE_STONE_PUSH / rightResistance;

  return {
    networkId,
    candidateStone,
    leftResistance,
    rightResistance,
    leftRiver,
    rightRiver,
    distinguishes: leftRiver !== rightRiver,
  };
}

export function createSingleStoneState(): SingleStoneState {
  return { matchedNetworkIds: [] };
}

export function recordSingleStoneMatch(
  state: SingleStoneState,
  comparison: SingleStoneComparison,
): SingleStoneState {
  if (
    comparison.distinguishes ||
    state.matchedNetworkIds.includes(comparison.networkId)
  ) {
    return state;
  }

  return {
    matchedNetworkIds: [...state.matchedNetworkIds, comparison.networkId],
  };
}

export function isSingleStoneSolved(state: SingleStoneState): boolean {
  return state.matchedNetworkIds.length >= SINGLE_STONE_REQUIRED_MATCHES;
}

 succeeded in 980ms:
export type LighthouseTank = 1 | 2 | 4;
export type LighthouseBrake = 1 | 2 | 4 | 8;
export type LighthouseTiming = 'fast' | 'just' | 'slow';
export type LighthousePhase = 'charging' | 'dumping';

export const LIGHTHOUSE_UNIT_MS = 250;
export const LIGHTHOUSE_TARGET_RHYTHM = 8;
export const LIGHTHOUSE_DISCHARGE_UNIT_MS = 100;
export const LIGHTHOUSE_MAX_BRIEF_DISCHARGE_BRAKE = 2;

export interface LighthouseState {
  tank: LighthouseTank;
  chargeBrake: LighthouseBrake;
  dischargeBrake: LighthouseBrake;
  level: number;
  phase: LighthousePhase;
  flashCount: number;
}

export interface LighthouseReading {
  rhythm: number;
  chargePeriodMs: number;
  dischargePeriodMs: number;
  timing: LighthouseTiming;
  briefDischarge: boolean;
  valid: boolean;
}

export function lighthouseRhythm(
  tank: LighthouseTank,
  chargeBrake: LighthouseBrake,
): number {
  return tank * chargeBrake;
}

export function lighthouseChargePeriodMs(
  tank: LighthouseTank,
  chargeBrake: LighthouseBrake,
): number {
  return LIGHTHOUSE_UNIT_MS * lighthouseRhythm(tank, chargeBrake);
}

export function lighthouseDischargePeriodMs(dischargeBrake: LighthouseBrake): number {
  return LIGHTHOUSE_DISCHARGE_UNIT_MS * dischargeBrake;
}

export function lighthouseTiming(
  tank: LighthouseTank,
  chargeBrake: LighthouseBrake,
): LighthouseTiming {
  const rhythm = lighthouseRhythm(tank, chargeBrake);
  if (rhythm < LIGHTHOUSE_TARGET_RHYTHM) return 'fast';
  if (rhythm > LIGHTHOUSE_TARGET_RHYTHM) return 'slow';
  return 'just';
}

export function isBriefDischarge(dischargeBrake: LighthouseBrake): boolean {
  return dischargeBrake <= LIGHTHOUSE_MAX_BRIEF_DISCHARGE_BRAKE;
}

export function isLighthouseSolution(
  tank: LighthouseTank,
  chargeBrake: LighthouseBrake,
  dischargeBrake: LighthouseBrake,
): boolean {
  return (
    lighthouseTiming(tank, chargeBrake) === 'just' &&
    isBriefDischarge(dischargeBrake)
  );
}

export function createLighthouseState(
  tank: LighthouseTank = 1,
  chargeBrake: LighthouseBrake = 1,
  dischargeBrake: LighthouseBrake = 1,
): LighthouseState {
  return {
    tank,
    chargeBrake,
    dischargeBrake,
    level: 0,
    phase: 'charging',
    flashCount: 0,
  };
}

export function configureLighthouse(
  state: LighthouseState,
  tank: LighthouseTank,
  chargeBrake: LighthouseBrake,
  dischargeBrake: LighthouseBrake,
): LighthouseState {
  return {
    ...state,
    tank,
    chargeBrake,
    dischargeBrake,
    level: 0,
    phase: 'charging',
  };
}

export function advanceLighthouse(state: LighthouseState, dtMs: number): LighthouseState {
  let remaining = Number.isFinite(dtMs) ? Math.max(0, dtMs) : 0;
  if (remaining === 0) return state;

  let next = { ...state };
  while (remaining > 0) {
    if (next.phase === 'charging') {
      const period = lighthouseChargePeriodMs(next.tank, next.chargeBrake);
      const untilThreshold = period * ((100 - next.level) / 100);
      if (remaining < untilThreshold) {
        next.level += (remaining / period) * 100;
        remaining = 0;
      } else {
        remaining -= untilThreshold;
        next.level = 100;
        next.phase = 'dumping';
        next.flashCount++;
      }
      continue;
    }

    const period = lighthouseDischargePeriodMs(next.dischargeBrake);
    const untilEmpty = period * (next.level / 100);
    if (remaining < untilEmpty) {
      next.level -= (remaining / period) * 100;
      remaining = 0;
    } else {
      remaining -= untilEmpty;
      next.level = 0;
      next.phase = 'charging';
    }
  }

  return next;
}

export function lighthouseReading(state: LighthouseState): LighthouseReading {
  const timing = lighthouseTiming(state.tank, state.chargeBrake);
  const briefDischarge = isBriefDischarge(state.dischargeBrake);
  return {
    rhythm: lighthouseRhythm(state.tank, state.chargeBrake),
    chargePeriodMs: lighthouseChargePeriodMs(state.tank, state.chargeBrake),
    dischargePeriodMs: lighthouseDischargePeriodMs(state.dischargeBrake),
    timing,
    briefDischarge,
    valid: timing === 'just' && briefDischarge,
  };
}

codex
La spec encaja con exports existentes: `clock` necesita estado local de predicción; `singlestone` ya tiene `equivalentResistance` y valores exportados; `lighthouse` ya trae toda la lectura. Voy a editar solo esas vistas y reutilizar clases existentes donde alcance.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 'src/styles.css','src/**/*.css' -Pattern 'bench-predict|distributor-district-state|lighthouse-actual-label|ok' -CaseSensitive:"'$false' in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Path 'src' -Recurse -Filter 'styles.css' | Select-Object -ExpandProperty FullName" in C:\YO\Proyectos\Roxana claude
 succeeded in 705ms:
C:\YO\Proyectos\Roxana claude\src\styles.css

 succeeded in 760ms:

src\styles.css:13:  --ok: #7ec46a;
src\styles.css:221:.ohm-widget[data-state='sobrecarga']::after { content: '☁'; position: absolute; margin-top: -78px; 
margin-left: 40px; color: #888; animation: smoke 1.2s infinite; font-size: 20px; }
src\styles.css:223:@keyframes smoke { from { transform: translateY(0); opacity: .9; } to { transform: 
translateY(-16px); opacity: 0; } }
src\styles.css:226:.wire { stroke: #5d5868; stroke-width: 6; fill: none; stroke-linecap: round; transition: stroke 
.4s; }
src\styles.css:227:.wire.live { stroke: var(--accent); filter: drop-shadow(0 0 4px rgba(255,211,77,.8)); }
src\styles.css:228:.wire.hot { stroke: var(--danger); filter: drop-shadow(0 0 6px rgba(226,86,58,.9)); }
src\styles.css:229:.gap-slot { fill: #211e29; stroke: #6b6478; stroke-width: 2; stroke-dasharray: 5 4; cursor: 
pointer; }
src\styles.css:230:.gap-slot:hover { stroke: var(--accent); }
src\styles.css:231:.gap-slot.bridged { fill: #8a7c50; stroke: var(--accent); stroke-dasharray: none; }
src\styles.css:232:.gap-slot.broken { fill: #2a1d1a; stroke: #7a4438; cursor: not-allowed; }
src\styles.css:377:.sleepingriver-progress span.done { color: var(--ok); border-color: rgba(126,196,106,.55); }
src\styles.css:448:.clock-stage[data-timing="just"] .clock-actual-label { color: var(--ok); }
src\styles.css:560:.lighthouse-stage[data-timing="fast"] .lighthouse-actual-label { color: #e7a45d; }
src\styles.css:561:.lighthouse-stage[data-timing="slow"] .lighthouse-actual-label { color: #8fa9c8; }
src\styles.css:562:.lighthouse-stage[data-timing="just"][data-discharge="dragged"] .lighthouse-actual-label {
src\styles.css:565:.lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
src\styles.css:566:  color: var(--ok);
src\styles.css:659:.warmth-progress span.done { color: var(--ok); border-color: rgba(126,196,106,.55); }
src\styles.css:705:.steps-progress span.done { color: var(--ok); border-color: rgba(126,196,106,.55); }
src\styles.css:726:  border-color: var(--ok); box-shadow: inset 0 0 0 1px rgba(126,196,106,.25);
src\styles.css:788:  color: var(--ok); border-color: rgba(126,196,106,.55);
src\styles.css:874:  border-color: var(--ok); box-shadow: inset 0 0 0 1px rgba(126,196,106,.25);
src\styles.css:1006:.infirmary-machine.stable { border-color: var(--ok); box-shadow: inset 0 0 18px 
rgba(126,196,106,.12); }
src\styles.css:1046:  border-color: rgba(126,196,106,.55); color: var(--ok);
src\styles.css:1066:  border-color: var(--ok); box-shadow: 0 0 18px rgba(126,196,106,.25);
src\styles.css:1132:  border-color: var(--ok); box-shadow: inset 0 0 18px rgba(126,196,106,.12);
src\styles.css:1140:.forge-trunk strong { color: var(--ok); }
src\styles.css:1195:.bench-predict {
src\styles.css:1199:.bench-predict.hidden { display: none; }
src\styles.css:1200:.bench-predict-q { margin: 0 0 10px; color: #e8dcc0; font-size: 14px; }
src\styles.css:1201:.bench-predict-row { display: flex; gap: 8px; flex-wrap: wrap; justify-content: center; }
src\styles.css:1202:.bench-predict-btn {
src\styles.css:1206:.bench-predict-btn:hover { border-color: var(--accent); background: #45382a; }
src\styles.css:1248:.branch-card.green { border-color: var(--ok); box-shadow: inset 0 0 0 1px rgba(126,196,106,.25); }
src\styles.css:1325:  border-color: var(--ok); box-shadow: inset 0 0 0 1px rgba(126,196,106,.25);
src\styles.css:1346:.distributor-district-state {
src\styles.css:1350:.distributor-district-state.ok { color: var(--ok); }
src\styles.css:1372:.bita-book {
src\styles.css:1525:  .bita-book { flex-direction: column; height: 94vh; }



exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/styles.css' -TotalCount 620 | Select-Object -Last 110" in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Path 'src/styles.css' -TotalCount 1370 | Select-Object -Last 70" in C:\YO\Proyectos\Roxana claude
 succeeded in 703ms:
  width: 54px; height: 33px; font-size: 8px;
}
.lighthouse-tank { position: relative; text-align: center; }
.lighthouse-tank[data-tank="1"] .tank { max-width: 95px; }
.lighthouse-tank[data-tank="2"] .tank { max-width: 115px; }
.lighthouse-tank[data-tank="4"] .tank { max-width: 135px; }
.lighthouse-threshold {
  position: absolute; z-index: 1; top: 22px; left: 50%; width: 105px;
  transform: translateX(-50%); border-top: 2px dashed #d66b4f;
  color: #d98b76; font-size: 9px; pointer-events: none;
}
.lighthouse-lens {
  display: grid; justify-items: center; gap: 7px; color: #a99; font-size: 10px;
}
.lighthouse-lens-glow {
  width: 58px; height: 58px; border: 4px solid #7993a2; border-radius: 50%;
  background: #344654; box-shadow: inset 0 0 18px rgba(118,152,168,.35);
}
.lighthouse-stage[data-phase="dumping"] .lighthouse-lens-glow {
  background: #fff0a4; box-shadow: 0 0 28px rgba(255,232,123,.95), inset 0 0 12px #fff;
}
.lighthouse-lake {
  height: 18px; margin-top: -14px; border-radius: 50%;
  background: #102a39; color: #7894a3; text-align: center; font-size: 9px; line-height: 18px;
}
.lighthouse-stage.restored .lighthouse-lens-glow,
.lighthouse-stage.restored .lighthouse-lake {
  animation: lighthouse-restored-flash 1.4s ease-out 2;
}
@keyframes lighthouse-restored-flash {
  0%, 100% { filter: brightness(1); }
  35% { filter: brightness(3); box-shadow: 0 0 34px rgba(255,232,123,.95); }
}
.lighthouse-reference {
  display: grid; grid-template-columns: 155px 1fr 120px 1fr;
  align-items: center; gap: 8px; padding: 8px 10px;
  border: 1px solid #4a4554; border-radius: 8px; background: #1d1a23;
}
.lighthouse-reference strong, .lighthouse-reference span { display: block; }
.lighthouse-reference strong { color: #d8cfc0; font-size: 12px; }
.lighthouse-reference div > span { color: #887f90; font-size: 10px; }
.lighthouse-reference-pulse, .lighthouse-actual-pulse {
  position: relative; height: 8px; overflow: hidden; border-radius: 4px; background: #342f3d;
}
.lighthouse-reference-pulse span, .lighthouse-actual-pulse span {
  position: absolute; top: 0; bottom: 0; width: 18px; border-radius: 4px;
  background: var(--accent); animation: clock-pulse 2000ms linear infinite;
}
.lighthouse-actual-pulse span { animation-duration: var(--lighthouse-period); }
.lighthouse-stage[data-timing="fast"] .lighthouse-actual-label { color: #e7a45d; }
.lighthouse-stage[data-timing="slow"] .lighthouse-actual-label { color: #8fa9c8; }
.lighthouse-stage[data-timing="just"][data-discharge="dragged"] .lighthouse-actual-label {
  color: #d98b76;
}
.lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
  color: var(--ok);
}
.lighthouse-controls {
  display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
}
.lighthouse-controls section {
  padding: 8px; border: 1px solid #4a4554; border-radius: 8px; background: #1d1a23;
}
.lighthouse-controls h3 {
  margin-bottom: 7px; color: #a99; font-size: 12px; font-weight: 500;
}
.lighthouse-tanks, .lighthouse-charge-brakes, .lighthouse-discharge-brakes {
  display: grid; grid-template-columns: repeat(4, 1fr); gap: 4px;
}
.lighthouse-tanks { grid-template-columns: repeat(3, 1fr); }
.lighthouse-choice, .lighthouse-brake {
  min-width: 0; padding: 4px; border: 1px solid #5d5868; border-radius: 7px;
  background: #26222e; color: #d8cfc0; cursor: pointer;
}
.lighthouse-choice strong, .lighthouse-choice span { display: block; }
.lighthouse-choice strong { font-size: 10px; }
.lighthouse-choice span { margin-top: 2px; color: #887f90; font-size: 9px; }
.lighthouse-choice.selected, .lighthouse-brake.selected {
  border-color: var(--accent); background: rgba(255,211,77,.08);
}
.lighthouse-choice:disabled, .lighthouse-brake:disabled { opacity: .55; cursor: default; }
.lighthouse-brake .piedra {
  width: 100%; min-width: 0; height: 29px; padding: 2px; font-size: 8px; pointer-events: none;
}

.fuse { width: min(300px, 100%); padding: 10px; border: 1px solid #6b6478; border-radius: 8px; background: #211e29; }
.fuse-heading { display: flex; justify-content: space-between; font-size: 12px; color: #d8cfc0; }
.fuse-threshold, .fuse-status { color: #a99; }
.fuse-scale { height: 16px; margin: 8px 0 5px; border-radius: 8px; background: #342f3d; position: relative; overflow: hidden; }
.fuse-green { position: absolute; inset: 0 auto 0 0; background: rgba(126,196,106,.4); }
.fuse-fill { position: absolute; inset: 3px auto 3px 0; border-radius: 6px; background: #e8dcc0; transition: width .35s; }
.fuse-mark { position: absolute; top: 0; bottom: 0; width: 3px; background: var(--accent); }
.fuse.warning .fuse-fill { background: #e8c33a; }
.fuse.burned { border-color: var(--danger); }
.fuse.burned .fuse-fill { background: var(--danger); }
.fuse.burned .fuse-status { color: var(--danger); font-weight: 700; }
.breakable-channel {
  width: min(300px, 100%); padding: 10px; border: 1px solid #6b6478;
  border-radius: 8px; background: #211e29;
}
.breakable-channel.warning { animation: channel-vibrate .14s linear infinite; }
.breakable-channel.warning .fuse-fill { background: #ff6a32; }
.breakable-channel.cut { border-color: var(--danger); }
.breakable-channel.cut .fuse-fill { background: var(--danger); }
.breakable-channel.cut .fuse-status { color: var(--danger); font-weight: 700; }
@keyframes channel-vibrate {
  25% { transform: translateX(1px); }
  75% { transform: translateX(-1px); }
}


 succeeded in 743ms:
  color: #a99; font-size: 10px; text-transform: uppercase; letter-spacing: .06em;
}
.distributor-crossing {
  width: 70px; height: 36px; margin: 0 auto 4px; display: grid; place-items: center;
  border-radius: 50%; border: 3px solid #8a7c50; background: #26222e;
  color: #d8cfc0; font-size: 11px;
}
.distributor-crossing-sum {
  margin: 0 auto 9px; min-height: 18px; text-align: center;
  font-size: 14px; color: var(--accent); font-variant-numeric: tabular-nums;
}
.distributor-crossing-sum.over { color: #ff7a5c; }
.distributor-crossing-sum .eq-hint {
  color: #887f90; font-size: 11px; font-variant-numeric: normal;
}
.distributor-trunk.live, .distributor-wire.live {
  background: var(--accent); box-shadow: 0 0 9px rgba(255,211,77,.7);
}
.distributor-districts { display: grid; grid-template-columns: repeat(3, 1fr); gap: 9px; }
.distributor-district {
  min-width: 0; padding: 8px; border: 1px solid #5d5868; border-radius: 9px;
  background: #211e29; text-align: center;
}
.distributor-district.green {
  border-color: var(--ok); box-shadow: inset 0 0 0 1px rgba(126,196,106,.25);
}
.distributor-district.wrong { border-color: rgba(205,102,82,.65); }
.distributor-district h3 { margin: 3px 0 5px; font-size: 13px; color: #d8cfc0; }
.distributor-wire {
  width: 6px; height: 20px; margin: -8px auto 3px; border-radius: 4px; background: #5d5868;
}
.distributor-emblem {
  width: 34px; height: 28px; margin: 0 auto 2px; border: 2px solid #8a7c50;
  border-radius: 7px; background: #342f3d;
}
.distributor-district.forja .distributor-emblem { box-shadow: inset 0 0 13px #8c3e2d; }
.distributor-district.campanario .distributor-emblem { border-radius: 50% 50% 7px 7px; }
.distributor-district.biblioteca .distributor-emblem {
  border-radius: 2px; background: linear-gradient(90deg, #342f3d 47%, #8a7c50 48% 52%, #342f3d 53%);
}
.distributor-district-gauge .gauge { max-width: 150px !important; margin: -5px auto -8px; }
.distributor-target, .distributor-river, .distributor-gauge-label {
  color: #a99; font-size: 10px;
}
.distributor-river { margin: 3px 0 7px; color: var(--accent); }
.distributor-district-state {
  margin: 2px 0 1px; font-size: 12px; font-weight: 600; color: #cd9a52;
  text-transform: uppercase; letter-spacing: .04em;
}
.distributor-district-state.ok { color: var(--ok); }
.distributor-district-detail { color: #a99; font-size: 10px; }
.distributor-stones { display: grid; grid-template-columns: repeat(2, 1fr); gap: 5px; }
.distributor-stones .piedra { min-width: 0; padding: 5px 3px; font-size: 10px; }
.distributor-stones .piedra[aria-disabled="true"] { pointer-events: none; opacity: .55; }
.distributor-instruments {
  display: flex; flex-direction: column; align-items: center; gap: 8px;
}
.distributor-trunk-gauge { text-align: center; }
.distributor-trunk-reading { margin-top: 3px; color: var(--accent); font-size: 11px; }
.distributor-fuse { margin: 10px auto 0; }
.distributor-probes .ohm-probes { justify-content: center; }
.distributor-stage.burned { animation: trunk-flash .35s ease-out; }
.distributor-stage.burned .distributor-emblem { opacity: .18; box-shadow: none; }

/* ---------- BitÃ¡cora ---------- */
#bitacora {
  position: absolute; inset: 0; z-index: 60;
  background: rgba(8,7,11,.9);
  display: flex; align-items: center; justify-content: center;
  padding: 12px;

codex
Voy a aplicar cambios puntuales: en `clock` bloqueo los controles hasta elegir predicción; en `singlestone` saco la lectura diferida y la vuelvo readout vivo; en `lighthouse` agrego dos readouts de estado con clase `ok` para los casos correctos.
2026-06-22T22:48:08.770698Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts:
const BRAKES: { key: string; value: ClockBrake }[] = [
  { key: 'marron', value: 1 },
  { key: 'roja', value: 2 },
  { key: 'amarilla', value: 4 },
  { key: 'gris', value: 8 },
];

const FAST_DIALOGUE = '<b>Farero:</b> Â«Ese reloj tomÃ³ cafÃ©. MÃ¡s despacio.Â»';
const SLOW_DIALOGUE =
  '<b>Farero:</b> Â«Ese reloj estÃ¡ por jubilarse. Un poco mÃ¡s de brÃ­o.Â»';
const JUST_DIALOGUE =
codex
El primer patch no entró por diferencias de codificación en textos acentuados. Voy a rehacerlo con contextos estructurales y líneas ASCII para no depender de cómo PowerShell muestra UTF-8.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$i=1; Get-Content -Path '"'src/puzzles/clock.ts' | ForEach-Object { '{0,4}: {1}' -f "'$i, $_; $i++ }' in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$i=1; Get-Content -Path '"'src/puzzles/singlestone.ts' | ForEach-Object { '{0,4}: {1}' -f "'$i, $_; $i++ }' in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$i=1; Get-Content -Path '"'src/puzzles/lighthouse.ts' | ForEach-Object { '{0,4}: {1}' -f "'$i, $_; $i++ }' in C:\YO\Proyectos\Roxana claude
 succeeded in 816ms:
   1: import { sfxBridge, sfxClick, sfxWin } from '../audio';
   2: import { benchActions, openBench } from '../ui/bench';
   3: import { createSimTick, piedraEl, PIEDRAS, setTankLevel, tankSVG } from './common';
   4: import {
   5:   advanceClock,
   6:   clockReading,
   7:   configureClock,
   8:   createClockState,
   9:   type ClockBrake,
  10:   type ClockTank,
  11:   type ClockTiming,
  12: } from './clockModel';
  13: 
  14: const TANKS: { value: ClockTank; label: string }[] = [
  15:   { value: 1, label: 'chico' },
  16:   { value: 2, label: 'mediano' },
  17:   { value: 4, label: 'grande' },
  18: ];
  19: const BRAKES: { key: string; value: ClockBrake }[] = [
  20:   { key: 'marron', value: 1 },
  21:   { key: 'roja', value: 2 },
  22:   { key: 'amarilla', value: 4 },
  23:   { key: 'gris', value: 8 },
  24: ];
  25: 
  26: const FAST_DIALOGUE = '<b>Farero:</b> Â«Ese reloj tomÃ³ cafÃ©. MÃ¡s despacio.Â»';
  27: const SLOW_DIALOGUE =
  28:   '<b>Farero:</b> Â«Ese reloj estÃ¡ por jubilarse. Un poco mÃ¡s de brÃ­o.Â»';
  29: const JUST_DIALOGUE =
  30:   '<b>Ohm:</b> Â«Otro estanque. El mismo tiempo.Â»<br/>' +
  31:   '<b>Farero:</b> Â«Ese. Ese es el tic. Lo reconozco aunque me despierten a las tres.Â»';
  32: 
  33: export function abrirClock(onSolved: () => void, practica = false): void {
  34:   openBench(
  35:     'El Reloj de Ohmdal',
  36:     'DevuÃ©lvale un tic justo al pueblo. Ritmo objetivo: 4.',
  37:     (bench) => {
  38:       let state = createClockState();
  39:       let solved = false;
  40: 
  41:       const stage = document.createElement('div');
  42:       stage.className = 'bench-stage clock-stage';
  43:       stage.innerHTML = `
  44:         <div class="clock-machine">
  45:           <div class="clock-source">fuente</div>
  46:           <div class="clock-channel">
  47:             <span class="clock-flow"></span>
  48:             <div class="clock-stone-slot"></div>
  49:           </div>
  50:           <div class="clock-tank" data-tank="1">
  51:             <span class="clock-threshold">umbral de volcado</span>
  52:             ${tankSVG()}
  53:           </div>
  54:           <div class="clock-pendulum" aria-label="PÃ©ndulo del Reloj">
  55:             <div class="clock-face">
  56:               <span class="clock-hand"></span>
  57:             </div>
  58:             <div class="clock-rod"></div>
  59:             <div class="clock-weight"></div>
  60:             <span>pÃ©ndulo</span>
  61:           </div>
  62:         </div>
  63:         <div class="clock-reference">
  64:           <div>
  65:             <strong>Referencia</strong>
  66:             <span>tic objetivo Â· ritmo 4</span>
  67:           </div>
  68:           <div class="clock-reference-pulse"><span></span></div>
  69:           <div class="clock-actual">
  70:             <strong>Reloj</strong>
  71:             <span class="clock-actual-label">adelanta</span>
  72:           </div>
  73:           <div class="clock-actual-pulse"><span></span></div>
  74:         </div>
  75:         <div class="clock-controls">
  76:           <section>
  77:             <h3>Estanque</h3>
  78:             <div class="clock-tanks"></div>
  79:           </section>
  80:           <section>
  81:             <h3>Freno del canal</h3>
  82:             <div class="clock-brakes"></div>
  83:           </section>
  84:         </div>`;
  85:       bench.root.appendChild(stage);
  86: 
  87:       const tankButtons: HTMLButtonElement[] = [];
  88:       const brakeButtons: HTMLButtonElement[] = [];
  89:       addTankChoices();
  90:       addBrakeChoices();
  91: 
  92:       const actions = benchActions(bench.root, [
  93:         { label: 'Alejarse', onClick: () => bench.close() },
  94:         {
  95:           label: 'Continuar',
  96:           primary: true,
  97:           onClick: () => bench.close(onSolved),
  98:         },
  99:       ]);
 100:       actions['Continuar'].classList.add('hidden');
 101: 
 102:       const tick = createSimTick((dtMs) => {
 103:         const previousStep = state.pendulumStep;
 104:         state = advanceClock(state, dtMs);
 105:         if (state.pendulumStep > previousStep) sfxBridge();
 106:         render();
 107:       });
 108:       bench.onClose(tick.stop);
 109: 
 110:       function addTankChoices(): void {
 111:         const host = stage.querySelector<HTMLElement>('.clock-tanks')!;
 112:         for (const tank of TANKS) {
 113:           const button = document.createElement('button');
 114:           button.type = 'button';
 115:           button.className = 'clock-choice';
 116:           button.dataset.tank = String(tank.value);
 117:           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
 118:           button.addEventListener('click', () => {
 119:             if (solved) return;
 120:             sfxClick();
 121:             state = configureClock(state, tank.value, state.brake);
 122:             render();
 123:             evaluateConfiguration();
 124:           });
 125:           host.appendChild(button);
 126:           tankButtons.push(button);
 127:         }
 128:       }
 129: 
 130:       function addBrakeChoices(): void {
 131:         const host = stage.querySelector<HTMLElement>('.clock-brakes')!;
 132:         for (const brake of BRAKES) {
 133:           const button = document.createElement('button');
 134:           button.type = 'button';
 135:           button.className = 'clock-brake';
 136:           button.dataset.brake = String(brake.value);
 137:           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
 138:           button.appendChild(piedraEl(brake.key));
 139:           button.addEventListener('click', () => {
 140:             if (solved) return;
 141:             sfxClick();
 142:             state = configureClock(state, state.tank, brake.value);
 143:             render();
 144:             evaluateConfiguration();
 145:           });
 146:           host.appendChild(button);
 147:           brakeButtons.push(button);
 148:         }
 149:       }
 150: 
 151:       function evaluateConfiguration(): void {
 152:         const timing = clockReading(state).timing;
 153:         bench.setStatus(feedbackFor(timing));
 154:         if (timing !== 'just' || practica || solved) return;
 155: 
 156:         solved = true;
 157:         sfxWin();
 158:         tankButtons.forEach((button) => {
 159:           button.disabled = true;
 160:         });
 161:         brakeButtons.forEach((button) => {
 162:           button.disabled = true;
 163:         });
 164:         actions['Alejarse'].classList.add('hidden');
 165:         actions['Continuar'].classList.remove('hidden');
 166:         bench.setStatus(
 167:           `${JUST_DIALOGUE}<br/><br/><b>Toda la plaza oye el tic.</b> ` +
 168:           'La voz que recuperÃ³ con la campana ahora tiene pulso.',
 169:         );
 170:       }
 171: 
 172:       function feedbackFor(timing: ClockTiming): string {
 173:         if (timing === 'fast') return FAST_DIALOGUE;
 174:         if (timing === 'slow') return SLOW_DIALOGUE;
 175:         return JUST_DIALOGUE;
 176:       }
 177: 
 178:       function render(): void {
 179:         const reading = clockReading(state);
 180:         setTankLevel(stage, state.level);
 181:         stage.dataset.timing = reading.timing;
 182:         stage.style.setProperty('--clock-period', `${reading.periodMs}ms`);
 183:         stage.querySelector<HTMLElement>('.clock-tank')!.dataset.tank = String(state.tank);
 184:         stage.querySelector<HTMLElement>('.clock-actual-label')!.textContent =
 185:           reading.timing === 'fast'
 186:             ? 'adelanta'
 187:             : reading.timing === 'slow'
 188:               ? 'atrasa'
 189:               : 'tic justo';
 190: 
 191:         const stoneSlot = stage.querySelector<HTMLElement>('.clock-stone-slot')!;
 192:         const brake = BRAKES.find((candidate) => candidate.value === state.brake)!;
 193:         stoneSlot.replaceChildren(piedraEl(brake.key));
 194:         stoneSlot.querySelector('.piedra')?.classList.add('in-slot');
 195: 
 196:         const side = state.pendulumStep % 2 === 0 ? -28 : 28;
 197:         stage.querySelector<HTMLElement>('.clock-rod')!.style.transform = `rotate(${side}deg)`;
 198:         stage.querySelector<HTMLElement>('.clock-weight')!.style.transform =
 199:           `translateX(${side * 1.25}px)`;
 200:         stage.querySelector<HTMLElement>('.clock-hand')!.style.transform =
 201:           `rotate(${state.pendulumStep * 30}deg)`;
 202: 
 203:         tankButtons.forEach((button) => {
 204:           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
 205:         });
 206:         brakeButtons.forEach((button) => {
 207:           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
 208:         });
 209:       }
 210: 
 211:       bench.setStatus(
 212:         'El Estanque se llena y vuelca solo. Cambie el Estanque o el freno para ajustar el tic.',
 213:       );
 214:       render();
 215:     },
 216:   );
 217: }

 succeeded in 812ms:
   1: import { sfxClick, sfxOk, sfxWin } from '../audio';
   2: import { benchActions, openBench } from '../ui/bench';
   3: import {
   4:   ohmWidgetHTML,
   5:   piedraEl,
   6:   PIEDRAS,
   7:   setOhmState,
   8: } from './common';
   9: import {
  10:   SINGLE_STONE_NETWORKS,
  11:   SINGLE_STONE_PUSH,
  12:   SINGLE_STONE_REQUIRED_MATCHES,
  13:   compareSingleStone,
  14:   createSingleStoneState,
  15:   isSingleStoneSolved,
  16:   recordSingleStoneMatch,
  17:   type SingleStone,
  18:   type SingleStoneNetwork,
  19:   type SingleStoneNetworkId,
  20: } from './singlestoneModel';
  21: 
  22: const STONES: SingleStone[] = ['marron', 'roja', 'amarilla', 'gris'];
  23: 
  24: const SOLVED_DIALOGUE =
  25:   '<b>Edda:</b> Â«La red entera se esconde dentro de una piedra. Y si Ohm no la distingueâ€¦Â»<br/>' +
  26:   '<b>Ohm:</b> Â«â€¦el rÃ­o tampoco. Una red es una piedra que todavÃ­a no terminaste de mirar.Â»<br/>' +
  27:   '<b>Guardiana:</b> Â«Entonces el valle enteroâ€¦ es una piedra. Una sola piedra que aprendÃ­ a temer.Â»';
  28: 
  29: export function abrirSingleStone(onSolved: () => void, practica = false): void {
  30:   openBench(
  31:     'La Piedra Ãšnica',
  32:     'Arme una red a la izquierda y encuentre la Ãºnica piedra que lleva el mismo rÃ­o a la derecha.',
  33:     (bench) => {
  34:       let networkId: SingleStoneNetworkId = SINGLE_STONE_NETWORKS[0].id;
  35:       let candidateStone: SingleStone = 'gris';
  36:       let state = createSingleStoneState();
  37:       let solved = false;
  38: 
  39:       const stage = document.createElement('div');
  40:       stage.className = 'bench-stage singlestone-stage';
  41:       stage.innerHTML = `
  42:         <div class="singlestone-workbench">
  43:           <section class="singlestone-side singlestone-left">
  44:             <h3>Red armable</h3>
  45:             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
  46:             <div class="singlestone-network" aria-live="polite"></div>
  47:             <div class="singlestone-reading" data-reading="left">RÃ­o: â€”</div>
  48:           </section>
  49:           <div class="singlestone-ohm">
  50:             ${ohmWidgetHTML('Ohm Â· manantial')}
  51:             <span>Empuje ${SINGLE_STONE_PUSH} a cada lado</span>
  52:           </div>
  53:           <section class="singlestone-side singlestone-right">
  54:             <h3>Una piedra</h3>
  55:             <div class="singlestone-slot"></div>
  56:             <div class="singlestone-stones" aria-label="Piedras Ãºnicas posibles"></div>
  57:             <div class="singlestone-reading" data-reading="right">RÃ­o: â€”</div>
  58:           </section>
  59:         </div>
  60:         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
  61:         <div class="singlestone-progress" aria-label="Equivalencias encontradas"></div>`;
  62:       bench.root.appendChild(stage);
  63: 
  64:       const networkButtons = new Map<SingleStoneNetworkId, HTMLButtonElement>();
  65:       const networkChoices = stage.querySelector<HTMLElement>('.singlestone-network-choices')!;
  66:       for (const network of SINGLE_STONE_NETWORKS) {
  67:         const button = document.createElement('button');
  68:         button.type = 'button';
  69:         button.className = 'singlestone-network-choice';
  70:         button.textContent = network.label;
  71:         button.addEventListener('click', () => selectNetwork(network.id));
  72:         networkChoices.appendChild(button);
  73:         networkButtons.set(network.id, button);
  74:       }
  75: 
  76:       const stoneButtons = new Map<SingleStone, HTMLButtonElement>();
  77:       const stonesHost = stage.querySelector<HTMLElement>('.singlestone-stones')!;
  78:       for (const stone of STONES) {
  79:         const button = document.createElement('button');
  80:         button.type = 'button';
  81:         button.className = 'singlestone-stone';
  82:         button.setAttribute('aria-label', PIEDRAS[stone].nombre);
  83:         button.appendChild(piedraEl(stone));
  84:         button.addEventListener('click', () => selectStone(stone));
  85:         stonesHost.appendChild(button);
  86:         stoneButtons.set(stone, button);
  87:       }
  88: 
  89:       const compareButton = stage.querySelector<HTMLButtonElement>('.singlestone-compare')!;
  90:       compareButton.addEventListener('click', compare);
  91: 
  92:       const actions = benchActions(bench.root, [
  93:         { label: 'Alejarse', onClick: () => bench.close() },
  94:         {
  95:           label: 'Continuar',
  96:           primary: true,
  97:           onClick: () => bench.close(onSolved),
  98:         },
  99:       ]);
 100:       actions['Continuar'].classList.add('hidden');
 101: 
 102:       function selectNetwork(nextNetworkId: SingleStoneNetworkId): void {
 103:         if (solved) return;
 104:         sfxClick();
 105:         networkId = nextNetworkId;
 106:         clearReadings();
 107:         render();
 108:         bench.setStatus('Red elegida. Falta encontrar su Piedra Ãšnica.');
 109:       }
 110: 
 111:       function selectStone(stone: SingleStone): void {
 112:         if (solved) return;
 113:         sfxClick();
 114:         candidateStone = stone;
 115:         clearReadings();
 116:         render();
 117:         bench.setStatus('Piedra colocada. Ohm puede comparar los dos rÃ­os.');
 118:       }
 119: 
 120:       function compare(): void {
 121:         if (solved) return;
 122:         sfxClick();
 123:         const comparison = compareSingleStone(networkId, candidateStone);
 124:         setReading('left', comparison.leftRiver);
 125:         setReading('right', comparison.rightRiver);
 126: 
 127:         if (comparison.distinguishes) {
 128:           setOhmState(stage, 'debil');
 129:           bench.setStatus(
 130:             `<b>Ohm:</b> Â«Izquierda: rÃ­o ${formatNumber(comparison.leftRiver)}. Derecha: rÃ­o ${formatNumber(comparison.rightRiver)}. Distingo.Â»`,
 131:           );
 132:           return;
 133:         }
 134: 
 135:         sfxOk();
 136:         state = recordSingleStoneMatch(state, comparison);
 137:         setOhmState(stage, 'estable');
 138:         render();
 139:         const matchingDialogue =
 140:           `<b>Ohm:</b> Â«Izquierda: rÃ­o ${formatNumber(comparison.leftRiver)}. ` +
 141:           `Derecha: rÃ­o ${formatNumber(comparison.rightRiver)}. No distingo. Son la misma piedra.Â»`;
 142: 
 143:         if (!practica && isSingleStoneSolved(state)) {
 144:           solved = true;
 145:           sfxWin();
 146:           disableWorkbench();
 147:           actions['Alejarse'].classList.add('hidden');
 148:           actions['Continuar'].classList.remove('hidden');
 149:           bench.setStatus(`${matchingDialogue}<br/><br/>${SOLVED_DIALOGUE}`);
 150:           return;
 151:         }
 152: 
 153:         bench.setStatus(matchingDialogue);
 154:       }
 155: 
 156:       function clearReadings(): void {
 157:         stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'RÃ­o: â€”';
 158:         stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'RÃ­o: â€”';
 159:         setOhmState(stage, 'inerte');
 160:       }
 161: 
 162:       function setReading(side: 'left' | 'right', value: number): void {
 163:         stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
 164:           `RÃ­o: ${formatNumber(value)}`;
 165:       }
 166: 
 167:       function disableWorkbench(): void {
 168:         stage.querySelectorAll<HTMLButtonElement>('button').forEach((button) => {
 169:           button.disabled = true;
 170:         });
 171:       }
 172: 
 173:       function render(): void {
 174:         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
 175:         renderNetwork(
 176:           stage.querySelector<HTMLElement>('.singlestone-network')!,
 177:           network,
 178:         );
 179:         const slot = stage.querySelector<HTMLElement>('.singlestone-slot')!;
 180:         slot.replaceChildren(piedraEl(candidateStone));
 181:         slot.querySelector('.piedra')?.classList.add('in-slot');
 182: 
 183:         for (const [id, button] of networkButtons) {
 184:           button.classList.toggle('selected', id === networkId);
 185:           button.classList.toggle('matched', state.matchedNetworkIds.includes(id));
 186:         }
 187:         for (const [stone, button] of stoneButtons) {
 188:           button.classList.toggle('selected', stone === candidateStone);
 189:         }
 190:         renderProgress();
 191:       }
 192: 
 193:       function renderProgress(): void {
 194:         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
 195:         progress.textContent =
 196:           `Redes que Ohm no distinguiÃ³: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
 197:       }
 198: 
 199:       bench.setStatus(
 200:         '<b>Guardiana:</b> Â«Este mural lleva aquÃ­ mÃ¡s que yo. Nunca lo entendÃ­. Una maraÃ±aâ€¦ igual a una piedra. Â¿Igual cÃ³mo?Â»<br/>' +
 201:         '<b>Ohm:</b> Â«Demostrable. TrÃ¡iganme una red. Yo decido si la distingo.Â»',
 202:       );
 203:       render();
 204:       clearReadings();
 205:     },
 206:   );
 207: }
 208: 
 209: function renderNetwork(host: HTMLElement, network: SingleStoneNetwork): void {
 210:   host.innerHTML = `
 211:     <div class="singlestone-source">manantial</div>
 212:     <div class="singlestone-wire"></div>
 213:     <div class="singlestone-series"></div>
 214:     <div class="singlestone-parallel">
 215:       <div class="singlestone-branch" data-branch="one"></div>
 216:       <div class="singlestone-branch" data-branch="two"></div>
 217:     </div>
 218:     <div class="singlestone-return">vuelta</div>`;
 219: 
 220:   const seriesHost = host.querySelector<HTMLElement>('.singlestone-series')!;
 221:   if (network.seriesStone) {
 222:     seriesHost.appendChild(piedraEl(network.seriesStone));
 223:     seriesHost.querySelector('.piedra')?.classList.add('in-slot');
 224:   } else {
 225:     seriesHost.remove();
 226:   }
 227: 
 228:   host.querySelectorAll<HTMLElement>('.singlestone-branch').forEach((branch) => {
 229:     branch.appendChild(piedraEl(network.parallelStone));
 230:     branch.querySelector('.piedra')?.classList.add('in-slot');
 231:   });
 232: }
 233: 
 234: function formatNumber(value: number): string {
 235:   return Number.isInteger(value)
 236:     ? String(value)
 237:     : value.toFixed(2).replace(/0+$/, '').replace(/\.$/, '');
 238: }

 succeeded in 819ms:
   1: import { sfxBridge, sfxClick, sfxWin } from '../audio';
   2: import { benchActions, openBench } from '../ui/bench';
   3: import { createSimTick, piedraEl, PIEDRAS, setTankLevel, tankSVG } from './common';
   4: import {
   5:   advanceLighthouse,
   6:   configureLighthouse,
   7:   createLighthouseState,
   8:   lighthouseReading,
   9:   type LighthouseBrake,
  10:   type LighthouseTank,
  11:   type LighthouseTiming,
  12: } from './lighthouseModel';
  13: 
  14: const TANKS: { value: LighthouseTank; label: string }[] = [
  15:   { value: 1, label: 'chico' },
  16:   { value: 2, label: 'mediano' },
  17:   { value: 4, label: 'grande' },
  18: ];
  19: const BRAKES: { key: string; value: LighthouseBrake }[] = [
  20:   { key: 'marron', value: 1 },
  21:   { key: 'roja', value: 2 },
  22:   { key: 'amarilla', value: 4 },
  23:   { key: 'gris', value: 8 },
  24: ];
  25: 
  26: const FAST_DIALOGUE =
  27:   '<b>Farero:</b> Â«Tartamudea. Ese no avisa: balbucea. MÃ¡s lento, que el barco tiene que verlo venir.Â»';
  28: const SLOW_DIALOGUE =
  29:   '<b>Farero:</b> Â«Casi se queda quieto. Un faro dormido no salva a nadie. Un poco mÃ¡s de pulso.Â»';
  30: const DRAGGED_DUMP_DIALOGUE =
  31:   '<b>Farero:</b> Â«El tiempo es ese, lo reconozcoâ€¦ pero el destello se arrastra como despedida. Tiene que volcar de golpe: la salida, casi libre.Â»';
  32: const JUST_DIALOGUE =
  33:   '<b>Farero:</b> Â«Ese. Ese es. â€¦Cuarenta aÃ±os afinando el oÃ­do para esta noche.Â» ' +
  34:   '<em>(se le llenan los ojos)</em><br/><br/>' +
  35:   '<b>Ohm</b> gira hacia la luz; su pecho parpadea al mismo ritmo una vez.<br/>' +
  36:   '<em>(silencio)</em><br/><br/>' +
  37:   '<b>Edda:</b> Â«Â¿Ohmâ€¦? Tu pecho. Late igual que el Faro.Â»<br/>' +
  38:   '<b>Ohm:</b> Â«Dato registrado. Sin explicaciÃ³n disponible. â€¦TodavÃ­a.Â»';
  39: 
  40: export function abrirLighthouse(onSolved: () => void, practica = false): void {
  41:   openBench(
  42:     'El latido del Faro',
  43:     'Llene despacio por un camino y vuelque de golpe por el otro. Ritmo objetivo: 8.',
  44:     (bench) => {
  45:       let state = createLighthouseState();
  46:       let solved = false;
  47: 
  48:       const stage = document.createElement('div');
  49:       stage.className = 'bench-stage lighthouse-stage';
  50:       stage.innerHTML = `
  51:         <div class="lighthouse-machine">
  52:           <div class="lighthouse-source">fuente</div>
  53:           <div class="lighthouse-channel lighthouse-charge-channel">
  54:             <span class="lighthouse-flow"></span>
  55:             <span class="lighthouse-path-label">canal de carga</span>
  56:             <div class="lighthouse-charge-slot"></div>
  57:           </div>
  58:           <div class="lighthouse-tank" data-tank="1">
  59:             <span class="lighthouse-threshold">umbral de volcado</span>
  60:             ${tankSVG()}
  61:           </div>
  62:           <div class="lighthouse-channel lighthouse-discharge-channel">
  63:             <span class="lighthouse-flow"></span>
  64:             <span class="lighthouse-path-label">camino de volcado</span>
  65:             <div class="lighthouse-discharge-slot"></div>
  66:           </div>
  67:           <div class="lighthouse-lens" aria-label="Lente del Faro">
  68:             <span class="lighthouse-lens-glow"></span>
  69:             <strong>lente</strong>
  70:           </div>
  71:         </div>
  72:         <div class="lighthouse-lake">lago negro</div>
  73:         <div class="lighthouse-reference">
  74:           <div>
  75:             <strong>Referencia</strong>
  76:             <span>latido objetivo Â· ritmo 8 Â· ~2 s</span>
  77:           </div>
  78:           <div class="lighthouse-reference-pulse"><span></span></div>
  79:           <div>
  80:             <strong>Faro</strong>
  81:             <span class="lighthouse-actual-label">demasiado rÃ¡pido</span>
  82:           </div>
  83:           <div class="lighthouse-actual-pulse"><span></span></div>
  84:         </div>
  85:         <div class="lighthouse-controls">
  86:           <section>
  87:             <h3>Estanque</h3>
  88:             <div class="lighthouse-tanks"></div>
  89:           </section>
  90:           <section>
  91:             <h3>Freno de carga</h3>
  92:             <div class="lighthouse-charge-brakes"></div>
  93:           </section>
  94:           <section>
  95:             <h3>Freno de volcado</h3>
  96:             <div class="lighthouse-discharge-brakes"></div>
  97:           </section>
  98:         </div>`;
  99:       bench.root.appendChild(stage);
 100: 
 101:       const tankButtons: HTMLButtonElement[] = [];
 102:       const chargeButtons: HTMLButtonElement[] = [];
 103:       const dischargeButtons: HTMLButtonElement[] = [];
 104:       addTankChoices();
 105:       addBrakeChoices('charge');
 106:       addBrakeChoices('discharge');
 107: 
 108:       const actions = benchActions(bench.root, [
 109:         { label: 'Alejarse', onClick: () => bench.close() },
 110:         {
 111:           label: 'Continuar',
 112:           primary: true,
 113:           onClick: () => bench.close(onSolved),
 114:         },
 115:       ]);
 116:       actions['Continuar'].classList.add('hidden');
 117: 
 118:       const tick = createSimTick((dtMs) => {
 119:         if (solved) return;
 120:         const previousFlash = state.flashCount;
 121:         state = advanceLighthouse(state, dtMs);
 122:         if (state.flashCount > previousFlash) sfxBridge();
 123:         render();
 124:       });
 125:       bench.onClose(tick.stop);
 126: 
 127:       function addTankChoices(): void {
 128:         const host = stage.querySelector<HTMLElement>('.lighthouse-tanks')!;
 129:         for (const tank of TANKS) {
 130:           const button = document.createElement('button');
 131:           button.type = 'button';
 132:           button.className = 'lighthouse-choice';
 133:           button.dataset.tank = String(tank.value);
 134:           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
 135:           button.addEventListener('click', () => {
 136:             if (solved) return;
 137:             sfxClick();
 138:             state = configureLighthouse(
 139:               state,
 140:               tank.value,
 141:               state.chargeBrake,
 142:               state.dischargeBrake,
 143:             );
 144:             render();
 145:             evaluateConfiguration();
 146:           });
 147:           host.appendChild(button);
 148:           tankButtons.push(button);
 149:         }
 150:       }
 151: 
 152:       function addBrakeChoices(path: 'charge' | 'discharge'): void {
 153:         const host = stage.querySelector<HTMLElement>(
 154:           path === 'charge'
 155:             ? '.lighthouse-charge-brakes'
 156:             : '.lighthouse-discharge-brakes',
 157:         )!;
 158:         const buttons = path === 'charge' ? chargeButtons : dischargeButtons;
 159:         for (const brake of BRAKES) {
 160:           const button = document.createElement('button');
 161:           button.type = 'button';
 162:           button.className = 'lighthouse-brake';
 163:           button.dataset.brake = String(brake.value);
 164:           button.setAttribute(
 165:             'aria-label',
 166:             `${PIEDRAS[brake.key].nombre}, freno ${brake.value} de ${path === 'charge' ? 'carga' : 'volcado'}`,
 167:           );
 168:           button.appendChild(piedraEl(brake.key));
 169:           button.addEventListener('click', () => {
 170:             if (solved) return;
 171:             sfxClick();
 172:             state = configureLighthouse(
 173:               state,
 174:               state.tank,
 175:               path === 'charge' ? brake.value : state.chargeBrake,
 176:               path === 'discharge' ? brake.value : state.dischargeBrake,
 177:             );
 178:             render();
 179:             evaluateConfiguration();
 180:           });
 181:           host.appendChild(button);
 182:           buttons.push(button);
 183:         }
 184:       }
 185: 
 186:       function evaluateConfiguration(): void {
 187:         const reading = lighthouseReading(state);
 188:         if (!reading.valid || practica || solved) {
 189:           bench.setStatus(feedbackFor(reading.timing, reading.briefDischarge));
 190:           return;
 191:         }
 192: 
 193:         solved = true;
 194:         tick.stop();
 195:         sfxWin();
 196:         stage.classList.add('restored');
 197:         [...tankButtons, ...chargeButtons, ...dischargeButtons].forEach((button) => {
 198:           button.disabled = true;
 199:         });
 200:         actions['Alejarse'].classList.add('hidden');
 201:         actions['Continuar'].classList.remove('hidden');
 202:         bench.setStatus(
 203:           '<b>El lago entero parpadea con la lente.</b><br/><br/>' + JUST_DIALOGUE,
 204:         );
 205:       }
 206: 
 207:       function feedbackFor(timing: LighthouseTiming, briefDischarge: boolean): string {
 208:         if (timing === 'fast') return FAST_DIALOGUE;
 209:         if (timing === 'slow') return SLOW_DIALOGUE;
 210:         if (!briefDischarge) return DRAGGED_DUMP_DIALOGUE;
 211:         return 'Ritmo de carga 8 y volcado breve. El Faro sostiene el latido restaurado.';
 212:       }
 213: 
 214:       function render(): void {
 215:         const reading = lighthouseReading(state);
 216:         setTankLevel(stage, state.level);
 217:         stage.dataset.timing = reading.timing;
 218:         stage.dataset.phase = state.phase;
 219:         stage.dataset.discharge = reading.briefDischarge ? 'brief' : 'dragged';
 220:         stage.style.setProperty('--lighthouse-period', `${reading.chargePeriodMs}ms`);
 221:         stage.querySelector<HTMLElement>('.lighthouse-tank')!.dataset.tank = String(state.tank);
 222:         stage.querySelector<HTMLElement>('.lighthouse-actual-label')!.textContent =
 223:           reading.timing === 'fast'
 224:             ? 'demasiado rÃ¡pido'
 225:             : reading.timing === 'slow'
 226:               ? 'demasiado lento'
 227:               : reading.briefDischarge
 228:                 ? 'latido justo'
 229:                 : 'volcado lento';
 230: 
 231:         renderStone('.lighthouse-charge-slot', state.chargeBrake);
 232:         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
 233:         tankButtons.forEach((button) => {
 234:           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
 235:         });
 236:         chargeButtons.forEach((button) => {
 237:           button.classList.toggle(
 238:             'selected',
 239:             button.dataset.brake === String(state.chargeBrake),
 240:           );
 241:         });
 242:         dischargeButtons.forEach((button) => {
 243:           button.classList.toggle(
 244:             'selected',
 245:             button.dataset.brake === String(state.dischargeBrake),
 246:           );
 247:         });
 248:       }
 249: 
 250:       function renderStone(selector: string, value: LighthouseBrake): void {
 251:         const slot = stage.querySelector<HTMLElement>(selector)!;
 252:         const brake = BRAKES.find((candidate) => candidate.value === value)!;
 253:         slot.replaceChildren(piedraEl(brake.key));
 254:         slot.querySelector('.piedra')?.classList.add('in-slot');
 255:       }
 256: 
 257:       bench.setStatus(
 258:         'Ajuste los dos caminos. El Farero responde en cuanto cambia el ritmo.',
 259:       );
 260:       render();
 261:     },
 262:   );
 263: }

2026-06-22T22:48:36.178258Z ERROR codex_core::tools::router: error=apply_patch verification failed: Failed to find expected lines in C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts:
const BRAKES: { key: string; value: ClockBrake }[] = [
  { key: 'marron', value: 1 },
  { key: 'roja', value: 2 },
  { key: 'amarilla', value: 4 },
  { key: 'gris', value: 8 },
];

const FAST_DIALOGUE = '<b>Farero:</b> Â«Ese reloj tomÃ³ cafÃ©. MÃ¡s despacio.Â»';
codex
Sigue chocando por el bloque con texto codificado. Cambio a parches todavía más pequeños, anclados solo en líneas ASCII y nombres de variables.
apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..509755af4eabf3f9dada18dcfb49c23e8db8656b
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..509755af4eabf3f9dada18dcfb49c23e8db8656b
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..48c023356e88b8282fed307b661503e916455d09
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..48c023356e88b8282fed307b661503e916455d09
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..bb0e72db5bb22e79a2d0a580617ab7b9b5b4738d
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,7 +39,12 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
 
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
+
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
       stage.innerHTML = `
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..bb0e72db5bb22e79a2d0a580617ab7b9b5b4738d
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,7 +39,12 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
 
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
+
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
       stage.innerHTML = `
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..b960a0ecc8cac53c7adbd5398ad27f4936c32b82
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..b960a0ecc8cac53c7adbd5398ad27f4936c32b82
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\clock.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..908efde6a5f78b54aeb592c9e670122709660a4b
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..908efde6a5f78b54aeb592c9e670122709660a4b
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,

codex
`clock` ya quedó con predicción inicial local y controles bloqueados hasta responder. Sigo con `singlestone`, manteniendo el botón como confirmación pero con ríos visibles permanentemente.
apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..5db0bda0ff5d07b688334248b5c178884590b630
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..5db0bda0ff5d07b688334248b5c178884590b630
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..d038e66de164f9ada669e05fbb420b38003826e9
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..d038e66de164f9ada669e05fbb420b38003826e9
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..1a8f867814c7e7d5b7ea5c53faf6f7018084e5d5
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..1a8f867814c7e7d5b7ea5c53faf6f7018084e5d5
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..6511db22964f5fae45451dec6c1ff42c8e94656c
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..6511db22964f5fae45451dec6c1ff42c8e94656c
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\singlestone.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

codex
`singlestone` ya calcula ambos ríos desde exports del modelo en cada render y cambió el contador. Ahora agrego los readouts legibles de `lighthouse` y un estilo mínimo para que el estado correcto se lea como en Repartidor.
apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\lighthouse.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..6852012aee6a37f5533db6294b2c4844b44334ad
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..6852012aee6a37f5533db6294b2c4844b44334ad
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\lighthouse.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..c19268adf004db083d5fb5af9876a1e3c9eaf853
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..c19268adf004db083d5fb5af9876a1e3c9eaf853
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\lighthouse.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..d7310dc38b66f1f6b61cd96af2bdc8940dc442d5
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..d7310dc38b66f1f6b61cd96af2bdc8940dc442d5
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\puzzles\lighthouse.ts
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }

codex
Los cambios de vista están aplicados. Falta el estilo chico para los dos readouts de `lighthouse`; después reviso diff y corro build.
apply patch
patch: completed
C:\YO\Proyectos\Roxana claude\src\styles.css
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

codex
Agregué solo estilos locales para los readouts del faro. Ahora verifico que no haya tocado nada fuera de las vistas indicadas y `styles.css`, y luego compilo.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git diff -- src/puzzles/clock.ts src/puzzles/singlestone.ts src/puzzles/lighthouse.ts src/styles.css' in C:\YO\Proyectos\Roxana claude
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'git status --short' in C:\YO\Proyectos\Roxana claude
 succeeded in 492ms:
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb1..253cda4 100644
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@ const BRAKES: { key: string; value: ClockBrake }[] = [
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@ export function abrirClock(onSolved: () => void, practica = false): void {
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@ export function abrirClock(onSolved: () => void, practica = false): void {
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@ export function abrirClock(onSolved: () => void, practica = false): void {
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@ export function abrirClock(onSolved: () => void, practica = false): void {
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@ export function abrirClock(onSolved: () => void, practica = false): void {
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc7..fcad97a 100644
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@ import {
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@ export function abrirLighthouse(onSolved: () => void, practica = false): void {
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@ export function abrirLighthouse(onSolved: () => void, practica = false): void {
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@ export function abrirLighthouse(onSolved: () => void, practica = false): void {
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa81024..9558ab5 100644
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@ import {
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@ export function abrirSingleStone(onSolved: () => void, practica = false): void {
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a..070bfc1 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@ button.bench-btn:disabled { opacity: .4; cursor: default; }
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }
warning: in the working copy of 'src/puzzles/clock.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/puzzles/lighthouse.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/puzzles/singlestone.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/styles.css', LF will be replaced by CRLF the next time Git touches it

 succeeded in 506ms:
 M package.json
 M src/puzzles/clock.ts
 M src/puzzles/lighthouse.ts
 M src/puzzles/singlestone.ts
 M src/styles.css
?? .claude/agents/
?? .claude/settings.json
?? .claude/workflows/
?? CLAUDE.md
?? codex-hito2.log
?? codex-lote-a.log
?? codex-lote-bc.log
?? docs/spec-lote-bc.md
?? files/
?? scripts/
warning: unable to access 'C:\Users\manue/.config/git/ignore': Permission denied
warning: unable to access 'C:\Users\manue/.config/git/ignore': Permission denied

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

codex
El diff queda limitado a `clock.ts`, `singlestone.ts`, `lighthouse.ts` y `styles.css`. Hay otros archivos ya modificados/no trackeados en el worktree que no toqué; ahora corro `npm run build`.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'npm run build' in C:\YO\Proyectos\Roxana claude
2026-06-22T22:50:02.929454Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.6 seconds
Output:
npm : No se puede cargar el archivo C:\Program Files\nodejs\npm.ps1 porque la ejecución de scripts está deshabilitada 
en este sistema. Para obtener más información, consulta el tema about_Execution_Policies en 
https:/go.microsoft.com/fwlink/?LinkID=135170.
En línea: 2 Carácter: 1
+ npm run build
+ ~~~
    + CategoryInfo          : SecurityError: (:) [], PSSecurityException
    + FullyQualifiedErrorId : UnauthorizedAccess

 exited 1 in 563ms:
npm : No se puede cargar el archivo C:\Program Files\nodejs\npm.ps1 porque la ejecución de scripts está deshabilitada 
en este sistema. Para obtener más información, consulta el tema about_Execution_Policies en 
https:/go.microsoft.com/fwlink/?LinkID=135170.
En línea: 2 Carácter: 1
+ npm run build
+ ~~~
    + CategoryInfo          : SecurityError: (:) [], PSSecurityException
    + FullyQualifiedErrorId : UnauthorizedAccess

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

codex
PowerShell bloqueó `npm.ps1` por política de ejecución. Voy a invocar `npm.cmd run build`, que evita ese wrapper y prueba el mismo script.
exec
"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'npm.cmd run build' in C:\YO\Proyectos\Roxana claude
 succeeded in 6567ms:

> proyecto-roxana@0.1.0 build
> tsc && vite build

[36mvite v6.4.3 [32mbuilding for production...[36m[39m
transforming...
[32m✓[39m 56 modules transformed.
rendering chunks...
computing gzip size...
[2mdist/[22m[32mindex.html                 [39m[1m[2m    1.91 kB[22m[1m[22m[2m │ gzip:   0.79 kB[22m
[2mdist/[22m[35massets/index-DxRyvmhe.css  [39m[1m[2m   60.28 kB[22m[1m[22m[2m │ gzip:  11.43 kB[22m
[2mdist/[22m[36massets/index-D8oseVLl.js   [39m[1m[2m1,936.92 kB[22m[1m[22m[2m │ gzip: 457.41 kB[22m
[32m✓ built in 3.32s[39m

diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

codex
Implementado.

- `src/puzzles/clock.ts`: agregué el panel inicial de predicción, bloqueo de Estanque/Freno hasta elegir, y los mensajes exactos según respuesta.
- `src/puzzles/singlestone.ts`: ambos ríos se calculan y muestran en vivo desde exports del modelo; el botón queda como confirmación; contador cambiado a `Equivalencias encontradas`.
- `src/puzzles/lighthouse.ts`: agregué readouts vivos de ritmo de carga y volcado con estados legibles.
- `src/styles.css`: estilo mínimo para los readouts del faro y estado `.ok`.

Verificación: `npm run build` quedó bloqueado por la política de PowerShell sobre `npm.ps1`, así que corrí `npm.cmd run build`; pasó correctamente (`tsc && vite build`).
diff --git a/src/puzzles/clock.ts b/src/puzzles/clock.ts
index b67fcb13ab21fdd351e09dcf7b6a2506c75e4227..253cda44382a25eac893e7097cf838d4a4528a86
--- a/src/puzzles/clock.ts
+++ b/src/puzzles/clock.ts
@@ -23,6 +23,8 @@
   { key: 'gris', value: 8 },
 ];
 
+type OpeningPrediction = 'rapido' | 'lento';
+
 const FAST_DIALOGUE = '<b>Farero:</b> «Ese reloj tomó café. Más despacio.»';
 const SLOW_DIALOGUE =
   '<b>Farero:</b> «Ese reloj está por jubilarse. Un poco más de brío.»';
@@ -37,6 +39,11 @@
     (bench) => {
       let state = createClockState();
       let solved = false;
+      let openingPrediction: OpeningPrediction | undefined;
+
+      const predict = document.createElement('div');
+      predict.className = 'bench-predict';
+      bench.root.appendChild(predict);
 
       const stage = document.createElement('div');
       stage.className = 'bench-stage clock-stage';
@@ -116,7 +123,7 @@
           button.dataset.tank = String(tank.value);
           button.innerHTML = `<strong>${tank.label}</strong><span>${tank.value}</span>`;
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, tank.value, state.brake);
             render();
@@ -137,7 +144,7 @@
           button.setAttribute('aria-label', `${PIEDRAS[brake.key].nombre}, freno ${brake.value}`);
           button.appendChild(piedraEl(brake.key));
           button.addEventListener('click', () => {
-            if (solved) return;
+            if (solved || !openingPrediction) return;
             sfxClick();
             state = configureClock(state, state.tank, brake.value);
             render();
@@ -148,6 +155,45 @@
         }
       }
 
+      function chooseOpeningPrediction(prediction: OpeningPrediction): void {
+        if (openingPrediction) return;
+        sfxClick();
+        openingPrediction = prediction;
+        predict.classList.add('hidden');
+        predict.innerHTML = '';
+        render();
+        bench.setStatus(
+          prediction === 'lento'
+            ? '<b>Farero:</b> «Bien pensado: más Estanque, más lento el tic. Ahora buscá el tic justo (ritmo 4).»'
+            : '<b>Farero:</b> «Al revés de lo que parece: más Estanque tarda más en llenarse, así que más lento. Comprobalo: buscá el tic justo (ritmo 4).»',
+        );
+      }
+
+      function renderPredict(): void {
+        if (openingPrediction) {
+          predict.classList.add('hidden');
+          predict.innerHTML = '';
+          return;
+        }
+
+        predict.classList.remove('hidden');
+        predict.innerHTML =
+          '<p class="bench-predict-q"><b>Ohm:</b> «Antes de tocar nada: si agrandás el Estanque, ¿el tic se vuelve más rápido o más lento?»</p>';
+        const row = document.createElement('div');
+        row.className = 'bench-predict-row';
+        for (const option of [
+          { key: 'rapido' as const, label: 'Más rápido' },
+          { key: 'lento' as const, label: 'Más lento' },
+        ]) {
+          const button = document.createElement('button');
+          button.className = 'bench-predict-btn';
+          button.textContent = option.label;
+          button.addEventListener('click', () => chooseOpeningPrediction(option.key));
+          row.appendChild(button);
+        }
+        predict.appendChild(row);
+      }
+
       function evaluateConfiguration(): void {
         const timing = clockReading(state).timing;
         bench.setStatus(feedbackFor(timing));
@@ -202,10 +248,13 @@
 
         tankButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.tank === String(state.tank));
+          button.disabled = solved || !openingPrediction;
         });
         brakeButtons.forEach((button) => {
           button.classList.toggle('selected', button.dataset.brake === String(state.brake));
+          button.disabled = solved || !openingPrediction;
         });
+        renderPredict();
       }
 
       bench.setStatus(
diff --git a/src/puzzles/lighthouse.ts b/src/puzzles/lighthouse.ts
index 4d91bc75bd34cd201ff44fbe60f048488886e01e..fcad97a0cc98416e3cca8ed0650ec28450202792
--- a/src/puzzles/lighthouse.ts
+++ b/src/puzzles/lighthouse.ts
@@ -5,6 +5,7 @@
   advanceLighthouse,
   configureLighthouse,
   createLighthouseState,
+  LIGHTHOUSE_TARGET_RHYTHM,
   lighthouseReading,
   type LighthouseBrake,
   type LighthouseTank,
@@ -82,6 +83,10 @@
           </div>
           <div class="lighthouse-actual-pulse"><span></span></div>
         </div>
+        <div class="lighthouse-conditions">
+          <div class="lighthouse-condition" data-condition="rhythm"></div>
+          <div class="lighthouse-condition" data-condition="discharge"></div>
+        </div>
         <div class="lighthouse-controls">
           <section>
             <h3>Estanque</h3>
@@ -227,6 +232,22 @@
               : reading.briefDischarge
                 ? 'latido justo'
                 : 'volcado lento';
+        const rhythmState =
+          reading.rhythm < LIGHTHOUSE_TARGET_RHYTHM
+            ? 'rápido'
+            : reading.rhythm > LIGHTHOUSE_TARGET_RHYTHM
+              ? 'lento'
+              : 'justo';
+        const rhythmCondition = stage.querySelector<HTMLElement>('[data-condition="rhythm"]')!;
+        rhythmCondition.textContent =
+          `Ritmo de carga: ${reading.rhythm} · objetivo ${LIGHTHOUSE_TARGET_RHYTHM} · ${rhythmState}`;
+        rhythmCondition.classList.toggle('ok', rhythmState === 'justo');
+
+        const dischargeState = reading.briefDischarge ? 'de golpe' : 'se arrastra';
+        const dischargeCondition = stage.querySelector<HTMLElement>('[data-condition="discharge"]')!;
+        dischargeCondition.textContent =
+          `Volcado: ${formatSeconds(reading.dischargePeriodMs)} s · ${dischargeState}`;
+        dischargeCondition.classList.toggle('ok', reading.briefDischarge);
 
         renderStone('.lighthouse-charge-slot', state.chargeBrake);
         renderStone('.lighthouse-discharge-slot', state.dischargeBrake);
@@ -261,3 +282,7 @@
     },
   );
 }
+
+function formatSeconds(ms: number): string {
+  return (ms / 1000).toFixed(1);
+}
diff --git a/src/puzzles/singlestone.ts b/src/puzzles/singlestone.ts
index fa810246d6da8f5fd14c5734b469589b798e0983..9558ab58d82b91cf5f6aa31e5d64be271c61a8d8
--- a/src/puzzles/singlestone.ts
+++ b/src/puzzles/singlestone.ts
@@ -10,8 +10,10 @@
   SINGLE_STONE_NETWORKS,
   SINGLE_STONE_PUSH,
   SINGLE_STONE_REQUIRED_MATCHES,
+  SINGLE_STONE_VALUES,
   compareSingleStone,
   createSingleStoneState,
+  equivalentResistance,
   isSingleStoneSolved,
   recordSingleStoneMatch,
   type SingleStone,
@@ -44,7 +46,7 @@
             <h3>Red armable</h3>
             <div class="singlestone-network-choices" aria-label="Redes objetivo"></div>
             <div class="singlestone-network" aria-live="polite"></div>
-            <div class="singlestone-reading" data-reading="left">Río: —</div>
+            <div class="singlestone-reading" data-reading="left">Río: 0</div>
           </section>
           <div class="singlestone-ohm">
             ${ohmWidgetHTML('Ohm · manantial')}
@@ -54,7 +56,7 @@
             <h3>Una piedra</h3>
             <div class="singlestone-slot"></div>
             <div class="singlestone-stones" aria-label="Piedras únicas posibles"></div>
-            <div class="singlestone-reading" data-reading="right">Río: —</div>
+            <div class="singlestone-reading" data-reading="right">Río: 0</div>
           </section>
         </div>
         <button class="singlestone-compare" type="button">Pedirle a Ohm que mida</button>
@@ -103,7 +105,7 @@
         if (solved) return;
         sfxClick();
         networkId = nextNetworkId;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Red elegida. Falta encontrar su Piedra Única.');
       }
@@ -112,7 +114,7 @@
         if (solved) return;
         sfxClick();
         candidateStone = stone;
-        clearReadings();
+        setOhmState(stage, 'inerte');
         render();
         bench.setStatus('Piedra colocada. Ohm puede comparar los dos ríos.');
       }
@@ -121,8 +123,7 @@
         if (solved) return;
         sfxClick();
         const comparison = compareSingleStone(networkId, candidateStone);
-        setReading('left', comparison.leftRiver);
-        setReading('right', comparison.rightRiver);
+        setReadings(comparison.leftRiver, comparison.rightRiver);
 
         if (comparison.distinguishes) {
           setOhmState(stage, 'debil');
@@ -153,15 +154,11 @@
         bench.setStatus(matchingDialogue);
       }
 
-      function clearReadings(): void {
-        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent = 'Río: —';
-        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent = 'Río: —';
-        setOhmState(stage, 'inerte');
-      }
-
-      function setReading(side: 'left' | 'right', value: number): void {
-        stage.querySelector<HTMLElement>(`[data-reading="${side}"]`)!.textContent =
-          `Río: ${formatNumber(value)}`;
+      function setReadings(leftRiver: number, rightRiver: number): void {
+        stage.querySelector<HTMLElement>('[data-reading="left"]')!.textContent =
+          `Río: ${formatNumber(leftRiver)}`;
+        stage.querySelector<HTMLElement>('[data-reading="right"]')!.textContent =
+          `Río: ${formatNumber(rightRiver)}`;
       }
 
       function disableWorkbench(): void {
@@ -172,6 +169,8 @@
 
       function render(): void {
         const network = SINGLE_STONE_NETWORKS.find((candidate) => candidate.id === networkId)!;
+        const leftRiver = SINGLE_STONE_PUSH / equivalentResistance(network);
+        const rightRiver = SINGLE_STONE_PUSH / SINGLE_STONE_VALUES[candidateStone];
         renderNetwork(
           stage.querySelector<HTMLElement>('.singlestone-network')!,
           network,
@@ -187,13 +186,14 @@
         for (const [stone, button] of stoneButtons) {
           button.classList.toggle('selected', stone === candidateStone);
         }
+        setReadings(leftRiver, rightRiver);
         renderProgress();
       }
 
       function renderProgress(): void {
         const progress = stage.querySelector<HTMLElement>('.singlestone-progress')!;
         progress.textContent =
-          `Redes que Ohm no distinguió: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
+          `Equivalencias encontradas: ${state.matchedNetworkIds.length} de ${SINGLE_STONE_REQUIRED_MATCHES}`;
       }
 
       bench.setStatus(
@@ -201,7 +201,6 @@
         '<b>Ohm:</b> «Demostrable. Tráiganme una red. Yo decido si la distingo.»',
       );
       render();
-      clearReadings();
     },
   );
 }
diff --git a/src/styles.css b/src/styles.css
index 642a61a29e23569e0961281b60b9d1d9ded68f05..070bfc16c1376aa7c24842b6ab11e4d225e14f3c
--- a/src/styles.css
+++ b/src/styles.css
@@ -565,6 +565,15 @@
 .lighthouse-stage[data-timing="just"][data-discharge="brief"] .lighthouse-actual-label {
   color: var(--ok);
 }
+.lighthouse-conditions {
+  display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px;
+}
+.lighthouse-condition {
+  padding: 7px 9px; border: 1px solid #4a4554; border-radius: 8px;
+  background: #1d1a23; color: #cd9a52; font-size: 12px; font-weight: 600;
+  text-transform: uppercase; letter-spacing: .04em;
+}
+.lighthouse-condition.ok { color: var(--ok); }
 .lighthouse-controls {
   display: grid; grid-template-columns: .8fr 1.25fr 1.25fr; gap: 8px;
 }

tokens used
121,279
Implementado.

- `src/puzzles/clock.ts`: agregué el panel inicial de predicción, bloqueo de Estanque/Freno hasta elegir, y los mensajes exactos según respuesta.
- `src/puzzles/singlestone.ts`: ambos ríos se calculan y muestran en vivo desde exports del modelo; el botón queda como confirmación; contador cambiado a `Equivalencias encontradas`.
- `src/puzzles/lighthouse.ts`: agregué readouts vivos de ritmo de carga y volcado con estados legibles.
- `src/styles.css`: estilo mínimo para los readouts del faro y estado `.ok`.

Verificación: `npm run build` quedó bloqueado por la política de PowerShell sobre `npm.ps1`, así que corrí `npm.cmd run build`; pasó correctamente (`tsc && vite build`).
