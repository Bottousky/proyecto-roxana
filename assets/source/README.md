# Fuentes 3D

Masters editables de assets nuevos. Sin Git LFS configurado, los binarios de esta carpeta se
mantienen fuera de Git y se registran por hash/ubicación en su manifiesto.

Los assets heredados de `assets/school3d/` no se mueven durante el setup.
