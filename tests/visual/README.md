# QA visual

- `fixtures/`: estados y datos deterministas.
- `baselines/`: capturas aprobadas y deliberadamente versionadas.
- `current/`: capturas de la ejecución actual, ignoradas.
- `diffs/`: diferencias regenerables, ignoradas.

Las capturas temporales de Playwright viven en `output/playwright/`.
