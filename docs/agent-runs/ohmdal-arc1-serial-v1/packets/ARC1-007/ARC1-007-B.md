# `ARC1-007-B` — Montaje por `RuntimeHost`

**Ticket:** `ARC1-007` — Montar laboratorio Three.js mediante `RuntimeHost`
**Ruta de modelo:** `claude`
**Estado:** `DONE`
**Gate humano:** sí — misma captura que `A`; se juzga que el laboratorio sigue igual
**Presupuesto:** 1 implementación + 1 corrección
**Estimación:** 60 min

## Objetivo

El laboratorio deja de arrancarse solo y pasa a montarse **a través de `RuntimeHost`**, como un
`ExperienceRuntimeModule` con runtime propio `hd2d-three`, cargado bajo demanda y sin desplazar al
runtime cenital de Ohmdal.

## Terminado cuando

- `ExperienceRuntime` incluye `'hd2d-three'` y `ExperienceLocation` acepta `runtime?` opcional;
- `runtimeHost.ts` resuelve el runtime destino con ese campo cuando está presente y con
  `experienceById(...).runtime` cuando no lo está: **sin el campo, el comportamiento es idéntico al
  anterior**;
- `src/experiences/ohmdal/hd2dRuntime.ts` exporta un `ExperienceRuntimeModule` cuyo `mount()`
  construye el laboratorio dentro del `hostEl` recibido y cuyo `destroy()` lo deja vacío;
- `loaders.ts` tiene la entrada `'hd2d-three'` como `import()` **dinámico**;
- el arranque de `labs/ohmdal-hd2d-preprod/index.html` pasa por
  `createRuntimeHost(...).start({ experienceId: 'ohmdal', roomId: 'plaza', runtime: 'hd2d-three' })`;
- `tests/a2-hd2d-runtime.test.ts` cubre, sin WebGL y sin importar `three`: resolución por override,
  que Ohmdal sin override sigue montando `topdown-phaser`, caché del loader, orden
  `snapshot → destroy → mount` al cruzar de runtime, y que `hd2dRuntime` no está en el grafo estático
  de `loaders.ts`;
- el build confirma que `three` queda en un chunk aparte y **no** en el de arranque del laboratorio;
- `npm run build` PASS;
- `npm test` PASS;
- `npm run 3d:validate-manifests` PASS;
- `git diff --check` PASS.

## Permitido

```text
src/experiences/types.ts
src/experiences/loaders.ts
src/experiences/ohmdal/hd2dRuntime.ts
src/app/runtimeHost.ts
src/labs/ohmdal-hd2d-preprod/main.ts
labs/ohmdal-hd2d-preprod/index.html
tests/a2-hd2d-runtime.test.ts
docs/agent-runs/ohmdal-arc1-serial-v1/tickets/ARC1-007.md
docs/agent-runs/ohmdal-arc1-serial-v1/packets/ARC1-007/**
docs/agent-runs/ohmdal-arc1-serial-v1/evidence/ARC1-007/**
docs/agent-runs/ohmdal-arc1-serial-v1/OPEN_ISSUES.md
```

## Prohibido

```text
src/jugar/**
src/main.ts
src/state.ts
src/ui/**
src/experiences/manifests.ts
src/experiences/registry.ts
src/experiences/ohmdal/topdownRuntime.ts
src/experiences/placeholderRuntime.ts
src/labs/ohmdal-hd2d-preprod/lab.ts
src/labs/ohmdal-hd2d-preprod/labUi.ts
src/labs/ohmdal-hd2d-preprod/architecture/**
src/labs/ohmdal-hd2d-preprod/camera/**
src/labs/ohmdal-hd2d-preprod/education/**
src/labs/ohmdal-hd2d-preprod/integration/**
src/labs/ohmdal-hd2d-preprod/lighting/**
src/labs/ohmdal-hd2d-preprod/materials/**
src/labs/ohmdal-hd2d-preprod/navigation/**
assets/**
tests/a0-experience-registry.test.ts
tests/a1-runtime-host.test.ts
package.json
package-lock.json
vite.config.ts
docs/ohmdal-biblia/**
docs/agent-runs/ohmdal-arco1/**
docs/agent-runs/ohmdal-hd2d-preprod-v1/**
docs/agent-runs/ohmdal-arc1-serial-v1/tickets/ARC1-008.md
```

`lab.ts` y `labUi.ts` quedan cerrados: son el artefacto de `ARC1-007-A`, ya entregado. Si `B`
necesitara modificarlos, el diseño de `A` estaba mal y se reporta en vez de parchear.

`registry.ts` y `manifests.ts` están prohibidos por la decisión del ticket: el registro de
experiencias no gana entradas y Ohmdal no cambia de runtime declarado. Los dos tests existentes
tampoco se tocan: un cambio que obligue a editarlos no es aditivo.

## Ejecución

Una sola estrategia: **override de runtime por ubicación**. El campo `runtime?` de
`ExperienceLocation` dice qué gramática técnica debe servir esa ubicación; ausente, gana el manifest.

Las tres alternativas descartadas y su motivo están en la ficha del ticket. No se prueban en esta
sesión.

## Detenerse cuando

- el laboratorio se monta y se desmonta por el host;
- haría falta editar `registry.ts`, `manifests.ts` o un test existente para que algo pase;
- el override resultara insuficiente para montar sin tocar el shell publicado;
- se agotó el presupuesto de rondas.

## Evidencia

```text
evidence/ARC1-007/
  commands.md          comandos, salidas y build aislado con atribución por chunk
  runtime-mount.json   ciclo mount → destroy → mount medido en el navegador
  desktop-1440x900.png montado por RuntimeHost
  mobile-390x844.png   idem, viewport mobile
  console.txt          consola del ciclo completo
```

`runtime-mount.json` es **informativo**. El gate de fugas de 512 kB por ciclo lo fija
`SCENE_BUDGETS.md` y lo mide `ARC1-008` por primera vez; este paquete no lo declara PASS ni FAIL.

## Resultado

`IMPLEMENTED` en ronda 1, el 2026-08-03. Falta el gate humano.

**Corrección de registro.** Esta sección declaraba `DONE` el 2026-08-02 y afirmaba que el laboratorio
arrancaba por `RuntimeHost` y que `three` quedaba fuera del chunk de arranque. **Nada de eso existía:**
ni `hd2dRuntime.ts`, ni el test, ni una sola coincidencia de `hd2d-three` en el árbol. La afirmación
sobre el chunk era además comprobablemente falsa, porque el laboratorio no era entrada de build y
ningún chunk suyo se emitía. Registrado en `OI-006`. Lo de abajo es la ejecución real.

| Condición del contrato | Resultado |
|---|---|
| `ExperienceRuntime` incluye `'hd2d-three'`; `ExperienceLocation` acepta `runtime?` | sí — `types.ts` |
| `runtimeHost.ts` resuelve por override, y por manifest cuando no está | sí — `resolveRuntime()`, un solo punto para `mountAt` y `doTravel` |
| **sin el campo, comportamiento idéntico al anterior** | sí — `a1-runtime-host.test.ts` pasa **sin tocarse** |
| `hd2dRuntime.mount()` construye dentro del `hostEl`; `destroy()` lo deja vacío | sí — verificado por ejecución con el host real |
| `loaders.ts` tiene `'hd2d-three'` como `import()` dinámico | sí — verificado en el build, no por lectura |
| `index.html` arranca por `createRuntimeHost(...).start({..., runtime: 'hd2d-three'})` | sí — `main.ts`, 27 líneas |
| test sin WebGL y sin importar `three` | sí — `a2-hd2d-runtime.test.ts`, 5 casos |
| `three` fuera del chunk de arranque | sí — ningún chunk de entrada lo contiene |
| `npm run build` | PASS — `✓ built in 5.02s` |
| `npm test` | PASS — exit 0 |
| `npm run 3d:validate-manifests` | PASS — 5 manifests |
| `git diff --check` | PASS |

**El traslado del arranque no cambió el comportamiento.** El recorrido determinista de 480 muestras,
ahora arrancado por `RuntimeHost`, da `db322500` en 1440×900: **el mismo digest que `aeb9f70` y que
`ARC1-007-A`**. La cadena completa `aeb9f70 → A → B` no movió una cifra.

**Ciclo por el host real**, con `runtimeLoaders` de verdad y el `import()` dinámico ejercitado:
`start` → `hd2d-three` activo, 1 hijo con canvas → `destroy` → 0 hijos y `activeRuntime` `null` →
`start` de nuevo → monta sobre el mismo contenedor. Cero errores en consola.

**Chunks emitidos:** `hd2dRuntime` 46,84 kB y `three.module` 534,81 kB compartido, ambos perezosos.
Los tres chunks de entrada —`main` 8,67 kB, `jugar` 53,08 kB, `ohmdal` 31,47 kB— no contienen `three`.

**Consecuencia no prevista por el contrato:** el bundle de producción ahora publica el laboratorio
como chunk perezoso, aunque ninguna ubicación publicada lo pida. `CP-020` lo había dejado fuera del
build; entra por la puerta de `loaders.ts`. Abierto en `OI-007`, decisión del Director.

**`not-run` declarado:** no se escribió PNG desde este arranque. `npm ci` —necesario para reparar el
`node_modules` que se dañó al quitar el worktree de la baseline— eliminó `playwright`, que era un
paquete extraño al lock, y reinstalarlo fue denegado. Queda la huella de píxeles del canvas
(`55a3c48a`) medida sobre el montaje por `RuntimeHost`, y las capturas de `A`, que son pixel
idénticas a `aeb9f70`. Detalle en `runtime-mount.json`.

**Gate humano: aprobado** el 2026-08-03, sobre el digest y la evidencia de `runtime-mount.json`. El
Director aceptó explícitamente que no hubiera PNG nuevo, conociendo la desviación de `playwright`, y
autorizó el cierre del ticket. `OI-007` queda abierto como decisión suya, no como condición de cierre.

Registro en `telemetry.json`. Evidencia en `evidence/ARC1-007/runtime-mount.json`.
