# ARC1-002 — Contrato H3 con base, presupuesto, ownership y autorización

**Estado inicial:** `READY`
**Owner:** Director
**Predecesor:** `ARC1-001 — DONE` (`b49b617`, veredicto `aprobar y avanzar`)
**Estimación canónica:** 0,5 d
**Implementación:** prohibida; este ticket sólo **redacta** un contrato y lo somete a autorización

## Objetivo verificable

Existe un documento de contrato H3 que fija los cuatro campos que el backlog canónico exige —base
fija, presupuesto, ownership y `executionAuthorized` explícito— y que enumera sin ambigüedad qué
queda prohibido. El documento es una propuesta hasta que el usuario la autorice.

## Alcance permitido

- leer la Biblia, el informe final de H1/H2 y el control plane;
- redactar `H3_CONTRACT.md`;
- reemplazar `ownership.json` por el contrato de este ticket;
- registrar decisiones y evidencia propias;
- someter al usuario las cuatro preguntas de autorización.

## Fuera de alcance

- fijar `executionAuthorized = true` sin respuesta humana;
- escribir `baseCommit` en `tasks.json` antes de la autorización;
- analizar o implementar `ARC1-003` y posteriores;
- cualquier cambio en `src/**`, `assets/**`, `tests/**` o `package*.json`.

## Ownership de escritura

- `docs/agent-runs/ohmdal-arc1-serial-v1/STATE.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/tasks.json`
- `docs/agent-runs/ohmdal-arc1-serial-v1/DECISIONS.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/ownership.json`
- `docs/agent-runs/ohmdal-arc1-serial-v1/H3_CONTRACT.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/tickets/ARC1-002.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/evidence/ARC1-002/**`

## Archivos prohibidos

`src/**`, `assets/**`, `tests/**`, `package*.json`, `docs/ohmdal-biblia/**`,
`docs/agent-runs/ohmdal-hd2d-preprod-v1/**`, `docs/agent-runs/ohmdal-arco1/**` y la ficha de
`ARC1-003`.

## Decisiones cerradas que hereda

- `CP-001` `STRICT-SERIAL`, WIP 1;
- `CP-002` sólo el Director cambia estados;
- `CP-003` gate humano para cambios visibles;
- `CP-004` cámara casi ortográfica, 4 direcciones, Ohm sprite;
- `CP-008` Meshy y generación paga con presupuesto cero;
- `CP-010` `ARC1-001` aprobado;
- `CP-011` equivalencia con DQ3 abierta y no bloqueante;
- `CP-012` layout/HUD mobile es deuda hacia `ARC1-026`.

## Preguntas humanas abiertas

1. ¿Se autoriza H3 con `baseCommit = b49b617`?
2. Android físico: **A** el usuario provee dispositivo, **B** se difiere a `ARC1-060` con todo claim
   mobile en `not-run`, o **C** no se abre H3.
3. ¿Se confirma el presupuesto, con Meshy y generación paga en cero y cero dependencias nuevas?
4. ¿Se confirma el ownership, con `src/jugar/**` protegido y el resto abierto ticket por ticket?

Sin respuesta explícita a las cuatro, `executionAuthorized` sigue en `false`.

## Riesgo principal

El informe final de H1/H2 condiciona la apertura de H3 a medir en Android físico antes de reclamar
30 fps. No hay dispositivo en este entorno. El contrato lo resuelve declarando el riesgo y ofreciendo
tres opciones, en vez de silenciarlo o declarar un PASS que no se midió.

## Condiciones exactas para `DONE`

- `H3_CONTRACT.md` existe y cubre base, alcance, prohibiciones, ownership, presupuesto, riesgo
  Android, referencia de calidad y condición de autorización;
- `ownership.json` refleja el ticket activo;
- el usuario respondió las cuatro preguntas;
- la respuesta quedó registrada en `DECISIONS.md` y `tasks.json`;
- build, tests, manifests y `git diff --check` PASS;
- diff dentro del ownership declarado;
- commit acotado creado;
- `ARC1-003` pasa a `READY` sólo si la autorización fue afirmativa.

## Rollback

No hay cambio de runtime. Si el contrato se autoriza por error, commit correctivo que restaure
`executionAuthorized = false` y `baseCommit = null`, con su entrada `CP-0NN`. No se borra evidencia
ni se reescribe historia.
