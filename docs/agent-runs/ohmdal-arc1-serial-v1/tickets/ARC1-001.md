# ARC1-001 — Aprobación visual de cámara corregida

**Estado inicial:** `HUMAN_REVIEW`
**Owner:** Director
**Implementación:** prohibida; este ticket sólo decide aceptar o pedir una corrección
**Rondas disponibles:** 1 devolución humana; no abrir tercera ronda automática

## Objetivo verificable

Confirmar que el harness conserva proporciones al redimensionar y que la cámara casi ortográfica
mantiene encuadre estable mediante zona muerta, sin perseguir cada paso ni deformar la imagen.

## Dependencias

- `CAM-FIX-001` implementado en `ade81bf` y `0e39d06`.
- Tests/evidencia registrados en `8784206`.
- No tiene ticket ARC1 predecesor.

## Alcance permitido

- leer el harness y la evidencia histórica;
- ejecutar servidor local y revisar 1440×900, 900×900 y 390×844;
- registrar decisión humana en `STATE.md`, `tasks.json`, `DECISIONS.md` y evidencia ARC1-001;
- si se rechaza, describir una única corrección acotada del mismo ticket.

## Ownership de escritura

Hasta el veredicto humano, ninguno. Tras decisión explícita del usuario, sólo:

- `docs/agent-runs/ohmdal-arc1-serial-v1/STATE.md`;
- `docs/agent-runs/ohmdal-arc1-serial-v1/tasks.json`;
- `docs/agent-runs/ohmdal-arc1-serial-v1/DECISIONS.md`;
- `docs/agent-runs/ohmdal-arc1-serial-v1/evidence/ARC1-001/**`.

## Archivos prohibidos

`src/**`, `assets/**`, `tests/**`, `package*.json`, `/jugar`, H3 y la ficha de `ARC1-002`.

## Decisiones cerradas

- proyección casi ortográfica;
- aspect ratio real: viewport angosto recorta horizontalmente y no estira;
- encuadres C1/C2/C3 con umbrales e histéresis;
- zona muerta antes de seguimiento;
- `reducedMotion` elimina viajes animados;
- cuatro direcciones y Ohm sprite.

## Preguntas humanas abiertas

1. ¿La imagen conserva proporciones al cambiar el tamaño del navegador?
2. ¿La cámara permanece quieta durante movimientos pequeños y corrige sólo al salir de la zona?
3. ¿Los cambios C1/C2/C3 se sienten intencionales y no constantes?
4. ¿Desktop angosto y mobile conservan sujeto, objetivo y consecuencia legibles?
5. ¿Aprobar, pedir una corrección concreta o descartar la dirección?

## Pruebas automáticas ya observadas

- `npm run build`: PASS, 185 módulos.
- `npm test`: PASS, incluida cámara/oclusión.
- `npm run 3d:validate-manifests`: PASS.
- `git diff --check`: PASS.

Se vuelven a ejecutar al validar este control plane; no sustituyen el gate humano.

## Prueba manual

1. Ejecutar `npm run dev -- --host 127.0.0.1`.
2. Abrir `http://127.0.0.1:5173/labs/ohmdal-hd2d-preprod/`.
3. Recorrer Portal→Plaza→Taller→Puerta en 1440×900.
4. Repetir o redimensionar a 900×900 y 390×844.
5. Observar proporciones, recorte/reencuadre, zona muerta, C1/C2/C3 y consola.

## Evidencia existente

- `docs/agent-runs/ohmdal-hd2d-preprod-v1/evidence/camera-correction/desktop-1440x900.png`
- `docs/agent-runs/ohmdal-hd2d-preprod-v1/evidence/camera-correction/desktop-narrow-900x900.png`
- `docs/agent-runs/ohmdal-hd2d-preprod-v1/evidence/camera-correction/mobile-390x844.png`
- estados deterministas bajo `evidence/camera-correction/default/`;
- consola Chrome: 0 errores/0 warnings en la captura histórica.

## Métricas observables

Resolución real, aspect ratio, vertical span, perfil desktop/mobile, anchor C1/C2/C3, posición del
target, zona muerta, `renderer.info`, consola y frame smoke. No extrapolar a Android físico.

## Condiciones exactas para `DONE`

- usuario aprueba explícitamente las cinco preguntas o emite veredicto equivalente;
- no hay P0/P1 ni deformación observada;
- build/tests/manifests/diff-check actuales pasan;
- decisión y evidencia se versionan;
- commit acotado creado;
- `ARC1-002` pasa de `BLOCKED` a `READY`, pero no se analiza ni implementa.

## Rollback

No hay cambio de runtime que revertir. Si el control se actualiza erróneamente, crear un commit
correctivo que restaure `HUMAN_REVIEW`; no borrar evidencia ni reescribir historia.
