# ARC1-004 — Congelar color script tarde→crepúsculo y shot deck del slice

**Estado inicial:** `READY`
**Owner:** Arquitectura
**Predecesor:** `ARC1-003 — DONE` (`88f669d`, canon visual congelado, `CP-015`; verificado en `CP-017`)
**Estimación canónica:** 1 d
**Tipo:** canon. Sin cambio de runtime.

## Objetivo verificable

Existen dos documentos congelados que cierran la tercera pata que doc. 15 §«Golden slice antes de
campaña» exige antes de producir arte:

1. **cómo se mueve el color de tarde a crepúsculo, y dónde** (`COLOR_SCRIPT.md`);
2. **dónde va cada cosa en el cuadro, con safe areas desktop/mobile** (`SHOT_DECK.md`).

Cada uno es modificable sólo con una decisión `CP-0NN` propia. Un ticket de escena no puede
redefinirlos para que su resultado pase.

## Por qué va acá y no dentro de `ARC1-024`

El harness ya implementa dos horas —`afternoon` y `twilight`— con valores concretos, pero **ninguna
regla escrita explica qué comunican**. Sin este ticket, `ARC1-024` (materiales, luz, DOF, agua y
VFX) tendría que decidir el lenguaje de color y la composición *dentro* de un ticket de escena, que
es exactamente lo que `CP-015` prohíbe: construir arte alrededor de decisiones todavía inestables y
después mover el canon para que el resultado pase.

`ARC1-003` congeló **qué debe ser legible**. Este ticket congela **con qué luz y en qué parte del
cuadro**.

## Subdivisión en paquetes

`PACKETS.md` criterio 2: estimación canónica 1 d > 90 min → se subdivide. Criterios 1, 3 y 4 no se
cumplen (un solo `owner`, ninguna implementación, plan por paquete de ≤10 pasos).

| Paquete | Artefacto | Estado |
|---|---|---|
| `ARC1-004-A` | `COLOR_SCRIPT.md` | contrato escrito en `packets/ARC1-004/ARC1-004-A.md` |
| `ARC1-004-B` | `SHOT_DECK.md` — shot deck y safe areas del slice | título tentativo; su contrato se escribe al abrirlo |

El commit es **uno solo, a nivel de ticket**, al cerrar ambos paquetes.

## Decisiones del Director previas al plan

| # | Pregunta | Respuesta, 2026-08-02 |
|---|---|---|
| 1 | ¿Qué congela el color script? | Reglas + beat map, citando los valores del harness como **línea base de blockout medida**, declarada como no-arte-final |
| 2 | ¿Las safe areas son vinculantes ya? | **Sí.** `CP-012` pasa de queja cualitativa a incumplimiento medible con destino `ARC1-026` |
| 3 | ¿Cómo se resuelve el review independiente? | Inline, con la desviación de `PACKETS.md` §«Frontera de sesión» **declarada en `telemetry.json`**, no simulada |

## Alcance permitido

- leer la Biblia, el harness de preproducción y el control plane;
- escribir `COLOR_SCRIPT.md` y `SHOT_DECK.md`;
- actualizar `ownership.json`, `STATE.md`, `tasks.json`, `DECISIONS.md`, `telemetry.json`;
- registrar evidencia propia.

## Fuera de alcance

- producir arte, texturas o capturas nuevas;
- **corregir** el HUD mobile de `CP-012` — se mide y se registra, se corrige en `ARC1-026`;
- fijar los valores destino de arte final: son de `ARC1-024`, con `CP-011` todavía abierto;
- modificar `docs/ohmdal-biblia/**`, canon protegido;
- enmendar `H3_CONTRACT.md`, `GOLDEN_FRAMES.md`, `IDENTITY.md` o `LEGAL_REFERENCES.md`;
- analizar o implementar `ARC1-005`.

## Ownership de escritura

`ownership.json` v5:

- `docs/agent-runs/ohmdal-arc1-serial-v1/COLOR_SCRIPT.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/SHOT_DECK.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/STATE.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/tasks.json`
- `docs/agent-runs/ohmdal-arc1-serial-v1/DECISIONS.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/ownership.json`
- `docs/agent-runs/ohmdal-arc1-serial-v1/telemetry.json`
- `docs/agent-runs/ohmdal-arc1-serial-v1/tickets/ARC1-004.md`
- `docs/agent-runs/ohmdal-arc1-serial-v1/packets/ARC1-004/**`
- `docs/agent-runs/ohmdal-arc1-serial-v1/evidence/ARC1-004/**`

## Archivos prohibidos

`src/**`, `assets/**`, `tests/**`, `package*.json`, `docs/ohmdal-biblia/**`,
`docs/agent-runs/ohmdal-hd2d-preprod-v1/**`, `docs/agent-runs/ohmdal-arco1/**`, `H3_CONTRACT.md`,
`GOLDEN_FRAMES.md`, `IDENTITY.md`, `LEGAL_REFERENCES.md` y la ficha de `ARC1-005`.

## Frontera con el canon de `ARC1-003`

Es la línea que más fácil se cruza sin querer:

| Documento | Fija |
|---|---|
| `GOLDEN_FRAMES.md` | **qué** debe ser legible en cada encuadre |
| `SHOT_DECK.md` | **dónde** va en el cuadro: composición, safe areas, parámetros por toma |
| `IDENTITY.md` | qué es y qué no es Ohmdal |
| `COLOR_SCRIPT.md` | **con qué luz y en qué punto del recorrido** |

Ante contradicción gana el canon de `ARC1-003` y se abre `CP-0NN`. El shot deck no puede redefinir
un golden frame; el color script no puede redefinir una regla de identidad.

## Condiciones exactas para `DONE`

- los dos documentos existen y son internamente consistentes;
- **cada número citado tiene archivo y línea de origen**, y ninguno difiere de su fuente;
- las safe areas están expresadas en % de viewport **y** en metros, con la conversión trazable al
  alto visible congelado de `GOLDEN_FRAMES.md` §3;
- el estado de medición del HUD está declarado —medido o `not-run`—, nunca inferido;
- la desviación de review está registrada en `telemetry.json`;
- build, tests, manifests y `git diff --check` PASS;
- diff dentro del ownership;
- commit acotado creado;
- `ownership.json` rotado a `ARC1-005` como parte del cierre (`CP-017`);
- `ARC1-005` pasa a `READY`.

## Gate humano

Ninguno obligatorio: no hay cambio visible. El Director puede objetar el contenido congelado en
cualquier momento; hacerlo genera una decisión `CP-0NN`, no una reapertura del ticket.

## Rollback

No hay cambio de runtime. Un congelamiento equivocado se corrige con un commit correctivo y su
entrada `CP-0NN`. No se borra evidencia ni se reescribe historia.
