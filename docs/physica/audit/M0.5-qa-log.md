# M0.5 QA Log — Physica slice

Format: ISO timestamp, cycle #, step (build | tests | playthrough), status, details.

---

## Cycle 1 — 2026-08-07T14:13Z

### Build — PASS
- `npm run build` completed in 26.25s, no TS errors.
- Final chunk sizes: `babylonRuntime` 8.25 MB, `tiles16` 1.66 MB, `physicaRuntime` 491 kB, `topdownRuntime` 355 kB.
- Vite chunk-size warning on `babylonRuntime` (>2 MB) — informational, not a failure.

### Tests — PASS
- `npm test` (node scripts/run-tests.mjs) finished clean.
- Last 16 lines: P5 plano inclinado OK, P5 referencia OK, P5 vector OK, R4 conectividad estricta OK (20 salas, 41 puertas, 134 things runtime), R8 progresión Arco I OK (5 unidades + cierre, ida y regreso), T0–T5 OK, W1–W7 OK.
- No failures, no skips.

### Playthrough — **FAIL (all 7 flags stayed false)**
- `node scripts/qa-physica-playthrough.mjs` completed end-to-end (note: first attempt failed at line 176 with canvas-size wait timeout on the post-clearSave reload; second attempt after a short pause ran clean).
- 0 page errors, 0 console errors during playthrough.
- Per-scene results (all flags after canonical action, all stayed `false`):
  - **Escena 2 cascada**: `cascadaObservada = false` (carried + with-pred-sube + launched — `piedrasInFlight = 0`).
  - **Escena 3 equilibrio**: `equilibrioResuelto = false` (picked up slab, placed, advanced 4 s).
  - **Escena 4 referencia**: `referenciaAnclada = false` (walked onto drifting platform at x≈38, pressed E).
  - **Escena 5 vector**: `vectorComun = false` (picked up first saquito at x≈54, advanced 2.5 s).
  - **Escena 6 plano**: `planoInclinado = false` (placed rampa, pushed rock at x≈70 for 5 s).
  - **Escena 7 estación**: `estacionEstabilizada = false` (preset `planoInclinado:true` survived reload — save/load OK — but pressing E near the station did not flip the flag).
  - **Escena 8 metrópoli**: `metropolisRevelada = false` (walked past station at x≈89, then teleported to (100, 20.5)).
- Screenshots: 27 qa-*.png written under `docs/physica/screenshots/`. `qa-flags-report.json` written with all canonical flags `false`.
- **Root-cause hypothesis (read-only, not yet verified)**: the in-DOM harness probes (`__pxPress`, `__pxTeleport`, `__pxSnapshot`) are wired in `babylonRuntime.ts` dev-only. `piedrasInFlight=0` after a launch suggests the press/teleport pipeline may not be reaching the world's input handler — either the probe is mounted to a different runtime than the Babylon scene, or the action key mapping (`act` → ?) doesn't match the in-world activator. The Q-flag `planoInclinado:true` surviving reload proves `localStorage` round-trip is healthy, so the bug is downstream of input → scene logic, not in the save layer.

---
