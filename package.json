{
  "name": "proyecto-roxana",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "for f in tests/*.test.ts; do node --experimental-strip-types \"$f\" || exit 1; done",
    "verify": "bash scripts/verificar-hito.sh"
  },
  "dependencies": {
    "@types/three": "^0.185.0",
    "phaser": "^4.1.0",
    "three": "^0.177.0"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^6.3.5"
  }
}
